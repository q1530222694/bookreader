# 不同文件 / 模块 的依赖关系（当前完整版 · 2026-07-14 更新）

> 本文件只保留「最新完整的依赖说明」，每次修改/新增文件后都会就地刷新对应条目。
> 架构铁律见 `docs/提示词.md`：禁止 `packages/` 反向依赖 `lib/`；UI 不硬编码颜色/字号/文案（走 `CupertinoColors.xxx` / `Theme` / `LocalizationEngine.text(...)`）；颜色变更必须上浮全局主题。

## 一、架构总览
- `lib/`（主工程 App）
  - `app/`：启动环境与全局注册中心
  - `core/`：主工程基础设施适配（主题引擎、字体管理）
  - `engine/`：鉴权引擎、服务端配置同步、本地化引擎
  - `features/shell/`：书架 / 阅读 / 回忆 / 设置等业务模块
  - `shared/`：全局 UI 组件
- `packages/`：跨应用复用中台 SDK（会员 / 支付 / 广告），本 App 当前未引入独立 packages 依赖，所有业务均在 `lib/` 内。

---

## 二、文件依赖明细（当前状态）

### `lib/features/shell/ui/bookshelf_page.dart`（书架主页面）
- **功能**：书架页 `CupertinoPageScaffold`，含顶部统计卡片、搜索、分类标签（全部/PDF/EPUB/TXT/其他）、最近阅读列表、导入与更多操作弹层。
- `_buildStatsCards`：顶部 4 项统计（全部 / 收藏 / 在读 / 已读），由 `_buildStatCell` 渲染；每项数据经构造函数传入、点击无回调（纯展示）。
  - **【2026-07-13 本次修改】**：① 卡片外边框 `Border.all(systemGrey5)` 移除，背景由 `systemBackground` 改为 `secondarySystemBackground.resolveFrom(context)`，阴影调淡为 `primaryColor.withOpacity(0.06)`，整体线框更柔和、与回忆页阅读统计卡片格式一致；② 删除 3 条 `systemGrey5` 灰色竖线分隔 `Container(width:1,height:46)`；③ 首项标签由「全部书籍」改为「全部」（走 `bookshelf_all_label`，其 zh 值已改为 '全部'）。
- `_buildRecentReading`：最近阅读书籍网格，点击跳转对应阅读器（按格式）。
- 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`（书籍数据源 `books`）
- 依赖/调用 ➔ `lib/engine/localization_engine.dart`（全部文案）
- 依赖/调用 ➔ `lib/features/shell/model/book_model.dart`（书籍模型）
- 跳转 ➔ `book_viewer_page.dart` / `epub_viewer_page.dart` / `txt_viewer_page.dart` / `comic_viewer_page.dart`（按格式打开；**【2026-07-14 三次修改】** 四个阅读器均改为传入 `bookId` + `controller`，并由 `ReadingSessionTracker` 记录每次阅读会话到 `ReadingSessionService`）

### `lib/features/shell/controller/bookshelf_controller.dart`（书架控制器）
- **功能**：书架「聪明大脑」，持有 `books`（`ValueListenable<List<BookModel>>`），负责导入、去重判断、阅读状态更新、进度回写；重复导入用短时 toast 提示。
- 依赖/调用 ➔ `lib/features/shell/service/bookshelf_service.dart`
- 消费 ➔ `lib/features/shell/model/book_model.dart`
- 被消费 ➔ `bookshelf_page.dart` / `memory_main_page.dart` / `memory_page.dart` / `reading_records_page.dart` / `forgotten_books_page.dart` / `book_viewer_page.dart`

### `lib/features/shell/service/bookshelf_service.dart`（书架持久化服务）
- **功能**：维护书架书籍列表与阅读进度（本地持久化）；**【2026-07-15 主线C】** 书架 PDF 封面缩略图生成 `_generatePdfCover` 由 pdfx 改为 **pdfrx**：`PdfDocument.openFile` → `document.pages[0]`（0-based）→ `page.render(width,height,backgroundColor: Colors.white)` → `pageImage.createImage()` → `uiImage.toByteData(format: ui.ImageByteFormat.png)` 取字节；结束 `document.dispose()` 释放句柄。
- 依赖 ➔ `lib/features/shell/model/book_model.dart` / `package:pdfrx`（PdfDocument/PdfPage/PdfImage）/ `dart:ui as ui`（ui.Image/ui.ImageByteFormat）。

### `lib/features/shell/model/book_model.dart`（书籍数据模型）
- **功能**：书籍实体（标题、封面、格式、进度 `progress`、是否收藏 `isFavorite`、`lastReadAt`、阅读时长等）。
- 变更 ➔ 增加 `copyWith()`，便于更新进度而不丢失元数据。

### `lib/features/shell/model/reading_stats_model.dart`（阅读统计模型）
- **功能**：由书籍列表聚合阅读统计数据，供回忆页与统计详情页复用。
- `fromBooks(books)`：聚合 `dailyMinutes`（按天）、`dailyBlockMinutes`（`Map<DateTime,List<int>>`，每天 4 个 6 小时段）、`hourlyMinutes`（按时段 0~23）、`dailyHourlyMinutes`（`Map<DateTime,List<int>>`，每天 24 小时档，供「日」视图趋势图/热力图）、**【2026-07-14 三次修改】** `dailyQuarterMinutes`（`Map<DateTime,List<int>>`，每天 96 段 = 24 小时×4 个 15 分钟段，按下标 `小时×4 + (分钟~/15)` 归桶，供「日」视图热力图每格=15 分钟的小方格着色）。
- `completedBooksInRange(books, startInclusive, endExclusive)`：区间内「读完」（`progress>=1.0` 且 `lastReadAt` 落在区间）的书籍数，统一口径。
- `activeDaysInRange(startInclusive, endExclusive)`：**【2026-07-14 新增】** 统计区间内「有阅读活动的天数」（累计阅读天数），供详情页新增指标卡使用。
- `minutesBetween` / `MonthTimelineRecord.monthTimeline(books)`：既有能力，未变。
- 依赖 ➔ `lib/features/shell/model/book_model.dart`
- 被消费 ➔ `memory_main_page.dart` / `memory_page.dart`

### `lib/engine/localization_engine.dart`（本地化引擎 · 全局唯一文案来源）
- 功能与全部相关键、2026-07-14 新增键，已合并至下方「home_page.dart」条目区域后的同文件说明，此处不再重复。

### `lib/features/shell/ui/memory_main_page.dart`（阅读回忆主页面）
- **功能**：长滚动回忆主页，卡片依次为：去年的今天、随机回忆、本周阅读时长（自真实数据+「比上周」）、阅读统计（周/月/年/全部 Tab + 4 项指标 + 右上「统计页」入口）、阅读时间轴（真实月度数据）、阅读热力图（4 象限=4 个 6 小时段、月份选择、主题主色派生色）、遗忘的书籍。
- 背景全部 `CupertinoColors.secondarySystemBackground.resolveFrom(context)`，跟随暗色模式；`_sectionIcon` 统一卡片标题前置图标。
- 依赖/调用 ➔ `bookshelf_controller.dart` / `localization_engine.dart` / `reading_stats_model.dart` / `forgotten_books_page.dart` / `memory_page.dart` / `reading_timeline_page.dart` / `timeline_entry.dart`

### `lib/features/shell/ui/memory_page.dart`（阅读统计详情页 · 二级）
- **功能**：完全按设计稿的「阅读统计详情」页：导航栏、周期选择器（日/周/月/年/全部，均分胶囊）、区间切换器（日/周/月/年 上下箭头、「全部」隐藏）、4 项指标卡（时长/本数/页数/笔记）+ **【2026-07-14 新增，同日二次修改】追加两项指标卡**：软件打开次数（`AppStatsService.getAppLaunchCountInRange(start,end)`，随统计区间联动）/ 累计阅读天数（`ReadingStats.activeDaysInRange`，随区间联动）、趋势图（条形/折线切换、日视图按 24 小时、其余按天/月、纵坐标自适应）、热力图（**【2026-07-14 三次修改·15 分钟小方格】** 日视图 = 4 行（0-6/6-12/12-18/18-24 六时段）× 每行 6 小时 × 每列 4 个 15 分钟小方格 = 96 个无数字小方格，颜色深浅对应该 15 分钟段阅读量，风格与周视图 4 段方块一致；周/月=4 象限日历、年/全部=GitHub 贡献图）、时间分布（时段/频率，日视图按时段用当天按小时明细）、阅读记录（**【2026-07-14 三次修改·会话级】** 概览：阅读次数/读完本数/读完耗时 + 阅读明细会话列表 + 读完了列表，数据来自 `ReadingSessionService`）。
- **【2026-07-14 三次修改·移除类型分布】**：删除「阅读类型分布」甜甜圈（含 `_buildTypeDistribution` / `_DonutSegment` / `_DonutPainter`），阅读统计详情页区块顺序调整为：指标卡 → 趋势图 → 热力图 → 时间分布 → 阅读记录。
- 颜色全走主题系统、文案全走 `LocalizationEngine`，无硬编码字面量。
- **【2026-07-14 变更】**：① `_StatsPeriod` 枚举新增 `day`，默认视图仍为 `month`；② 新增 `_selectedDay` 状态与「日」区间切换（上/下一天）；③ 新增 `_buildExtraStatCards`（打开次数+累计阅读天数，复用 `_MetricTile`）；④ 趋势图 `_trendEntries` 在「日」视图返回 24 个小时数据点，并为 `_TrendBarChart`/`_TrendLineChart` 增加可选 `xLabelBuilder`（日视图按小时标签）；**【同日二次修改·紧凑化】** ③ 追加指标卡「打开次数」由 `getAppLaunchCount()`（全局）改为 `getAppLaunchCountInRange(start,end)`；⑤ 热力图 `_buildDayHeatGrid` **【三次修改·15 分钟小方格】** 重写为「4 行 × 6 小时 × 4 个 15 分钟段小方格」，**删除 `_hourColor`、新增 `_quarterColor`（按 15 分钟段阈值派生颜色）**，数据源自模型新增的 `dailyQuarterMinutes`；⑥ 时间分布「时段」在「日」视图改用 `dailyHourlyMinutes[选中日]`；**⑦ 阅读记录 `_buildMonthlyRecords` 重写为会话级**：新增 `_buildRecordSummary`（阅读次数/读完本数/读完耗时三块统计）、`_RecordStatTile`、`_SessionRow`（书名 + HH:MM 开始 · 读了 Y 分钟），数据来自 `ReadingSessionService.sessionsInRange(start,end)` 与书籍 `progress>=1.0` 判定读完。
- 依赖/调用 ➔ `bookshelf_controller.dart` / `book_model.dart` / `reading_stats_model.dart` / `localization_engine.dart` / `app_stats_service.dart`（读打开次数：`getAppLaunchCountInRange` 区间统计）/ `reading_session_service.dart`（读阅读会话：`sessionsInRange`/`sessionsNotifier`，记录阅读次数与明细）/ `reading_records_page.dart` / 各阅读器页

### `lib/features/shell/service/app_stats_service.dart`（应用级统计服务 · 打开次数持久化）
- **功能**：维护应用级统计数据，本地 JSON（`app_stats.json`）持久化；`main.dart` 启动时调用 `incrementAppLaunchCount()` 记录打开。
- **【2026-07-14 二次修改·区间统计】**：① 新增 `launchTimestampsNotifier`（`ValueNotifier<List<DateTime>>`），记录每次应用打开的时间戳，随 `app_stats.json` 持久化（`incrementAppLaunchCount()` 每次打开追加 `DateTime.now()`）；② 新增 `getAppLaunchCountInRange(start,end)`：统计区间 `[start,end)` 内的打开次数（与阅读统计口径一致）；③ 既有 `getAppLaunchCount()`（全局累计）、`resetAppLaunchCount()` 保留。
- 依赖 ➔ `path_provider`（应用文档目录）/ `dart:convert`（JSON）
- 被消费 ➔ `home_page.dart`（展示累计打开次数）/ `memory_page.dart`（`_buildExtraStatCards` 按区间统计打开次数）

### `lib/features/shell/service/reading_session_service.dart`（阅读会话日志服务 · 新增【2026-07-14】）
- **功能**：持久化每次阅读会话（打开阅读器到离开的一次连续阅读），供「阅读记录」展示会话级数据（几点开始、读了多久、是否读完）。`ReadingSession`（`bookId` / `startedAtMs` / `durationSeconds` / `finished`）；`ReadingSessionService`（`static ValueNotifier<List<ReadingSession>> sessionsNotifier`、静态 `initialize()` 加载 `reading_sessions.json`、静态 `logSession(...)` 记录并落盘、`sessionsOnDay` / `sessionsInRange` / `finishedBookIdsInRange` 查询）；`ReadingSessionTracker`（各阅读器生命周期内计时，自动 `logSession`）。
- 持久化：本地 JSON（`reading_sessions.json`，应用文档目录，`path_provider`），与 `app_stats_service.dart` 同模式；`main.dart` 启动时调用 `ReadingSessionService.initialize()`。
- 依赖 ➔ `path_provider` / `dart:convert` / `localization_engine.dart`（时长格式化）
- 被消费 ➔ `memory_page.dart`（`_buildMonthlyRecords` 会话级阅读记录）/ `reading_records_page.dart`（全部会话明细）/ 各阅读器页（`ReadingSessionTracker` 记录会话）

### `lib/features/shell/ui/forgotten_books_page.dart`（遗忘的书籍二级页）
- **功能**：展示全部未读完书籍（`progress < 1.0`），按未打开天数倒序；响应式 `GridView`；复用 `_openBook` 跳转阅读器。
- 依赖/调用 ➔ `bookshelf_controller.dart` / `book_model.dart` / `localization_engine.dart`

### `lib/features/shell/ui/reading_records_page.dart`（全部阅读记录二级页）
- **功能**：**【2026-07-14 三次修改·会话级】** 展示全部阅读会话（每次打开阅读器即一条，含书名 + 开始时间 + 本次阅读时长，数据来自 `ReadingSessionService.sessionsNotifier`），顶部概览（阅读次数/读完本数/读完耗时），「阅读明细」为 `_SessionListRow` 会话列表，「读完了」为 `ReadingRecordRow`（按 `progress>=1.0`）。
- 依赖/调用 ➔ `bookshelf_controller.dart` / `book_model.dart` / `localization_engine.dart` / `reading_session_service.dart`（读会话：`sessionsNotifier` / `formatSessionTime` / `formatSessionDuration`）

### `lib/features/shell/ui/reading_timeline_page.dart` + `lib/features/shell/ui/timeline_entry.dart`
- **功能**：阅读时间轴「查看全部」页；`TimelineEntry` 无状态条目组件（颜色由调用方以主题派生色传入），`timelineEntryFromRecord` 拼装标题/摘要/详情。
- 依赖 ➔ `reading_stats_model.dart`（MonthTimelineRecord） / `localization_engine.dart`

### `lib/features/shell/ui/reader_settings_sheet.dart`（阅读设置底部抽屉）
- **功能**：底部抽屉式阅读设置（主题色块 / 亮度 / 字体 / 翻页 / 导航）；PDF 模式仅渲染底部 5 个功能按钮。
- 被调用 ➔ `txt_viewer_page.dart` / `book_viewer_page.dart`

### `lib/features/shell/ui/txt_viewer_page.dart`（TXT 阅读器）
- **功能**：默认全屏阅读，点击内容区弹出 `reader_settings_sheet`；修复了设置弹窗漏传 `onClose` 回调。**【2026-07-14 三次修改】** 接入 `ReadingSessionTracker`：initState 计时、dispose/应用退后台结束并 `logSession`，同时 `updateBookReadingDuration` 累加时长、按 `progress>=1.0` 判定本次是否读完。
- 依赖/调用 ➔ `reader_settings_sheet.dart` / `bookshelf_controller.dart` / `reading_session_service.dart`（记录会话）
- **本次修改（2026-07-16 上午 · 5 新功能续改）**：`ReaderSettingsSheet` 构造补传新增必填参数（`bookId:'txt_reader'`、`totalPages:0`、`currentPage:1`、`onJumpToPage:(_){}`、`onToggleLandscape:(_){}` 占位空实现，保证非 PDF 读者编译通过且进度/搜索/书签面板不展示）。

### `lib/features/shell/ui/book_viewer_page.dart`（通用 / PDF 阅读器）
- **功能**：打开 PDF/通用书籍；修复设置弹窗 `onClose`；PDF 翻页同步页码回书架进度；保留顶部返回与标题栏，移除底部翻页栏（仅点中间唤起设置时显示）；PDF 设置仅显示 5 按钮。**【2026-07-14 三次修改】** 在既有 `_pauseSessionAndPersist` 中追加 `ReadingSessionService.logSession`（开始时间/时长/是否读完），与既有 `updateBookReadingDuration` 并行。
- 依赖/调用 ➔ `bookshelf_controller.dart` / `reader_settings_sheet.dart` / `reading_session_service.dart`（记录会话）

### `lib/features/shell/ui/home_page.dart`（主页）
- **功能**：主页 4 张阅读统计卡片（本月阅读 / 今年阅读 / 累计阅读 / 打开次数）+ 「每日一句」区。
  - **【2026-07-14 本次修改】**：① 4 张统计卡片底色由 `secondarySystemBackground` 改为 `scaffoldBackgroundColor`、移除边框、阴影统一为 `systemGrey.withOpacity(0.06)` blurRadius 12 offset(0,6)，与主页「阅读统计」大卡片（`_readingDataSection` 顶部卡）格式一致；② 「每日一句」内容框同样改为 `scaffoldBackgroundColor`+去边框+同款柔和阴影；③ 标题行右侧新增刷新按钮（`CupertinoIcons.refresh`），点击调用 `DailySentenceService.refreshDisplaySentence()`；④ 展示文案监听 `DailySentenceService.displaySentenceNotifier`，并随开关 `SettingsController.dailySentenceUseBuiltin` 与自定义列表 `DailySentenceService.sentencesNotifier` 联动（initState 注册监听、dispose 移除）；⑤ 移除 `_buildStatCard` 未使用的 `accentColor` 死参。
  - **【2026-07-14 三次修改·批量增加】**：⑥ `_quickAddDailySentence` 快捷新增现按回车/换行拆分多行文本调用 `DailySentenceController.addSentencesBatch` 批量新增（与编辑页一致，一行一个每日一句）。
- 依赖/调用 ➔ `bookshelf_controller.dart` / `localization_engine.dart` / `app_stats_service.dart` / `daily_sentence_service.dart` / `settings_controller.dart`（监听开关）

### `lib/features/shell/model/daily_sentence_builtin.dart`（内置每日一句数据 · 新增）
- **功能**：纯数据文件，常量 `builtinReadingSentences`（`List<String>`，≥120 条关于「阅读」的中文经典句子）；开启「内置每日一句」时按日期稳定展示、刷新时随机换一句。
- 说明：按 `提示词.md` 的 i18n 豁免规则，视为「书籍正文类策展内容」，仅提供中文版、不参与多语言翻译（与用户自定义句、书籍内容一致）。
- 被消费 ➔ `daily_sentence_service.dart`（直接引用）

### `lib/features/shell/service/daily_sentence_service.dart`（每日一句持久化 + 取句服务）
- **功能**：维护用户自定义每日一句列表（本地 JSON 持久化）；**新增** `displaySentenceNotifier`（`ValueNotifier<String>`，主页展示句，不持久化）；`_selectSentence(useBuiltin,{refresh,current})` 按开关从内置池或自定义列表取句；`initDisplaySentence()`（按日期稳定初始化）、`refreshDisplaySentence()`（随机换一句）、`syncDisplaySentence()`（开关/列表变化时同步）；**【2026-07-14 二次修改】** 新增 `deleteSentence(id)`（静态，按 id 删除并持久化）、`reorderSentence(oldIndex,newIndex)`（静态，拖拽排序并持久化）；`_saveSentences` 由实例方法改为 `static`，供上述静态方法调用。**【2026-07-14 三次修改·删除崩溃修复 + 批量增加】** 新增 `addSentencesBatch(List<String>)`（静态：按行拆分、过滤空行、用「微秒时间戳 + 自增盐值 `_idSalt`」生成唯一 id 逐条批量新增并一次性落盘，返回实际新增条数）；`deleteSentence`/`addSentence`/`addSentencesBatch`/`updateSentence` 统一改为「**先更新内存 `sentencesNotifier` 再 `_saveSentences`**」并包 `try/catch` 防御，落盘异常只 `debugPrint` 不再抛出，避免异步异常导致界面崩溃（与 `daily_sentence_page.dart` 不用 shrinkWrap，改用原生 ListView.builder 彻底修复删除崩溃）。
- 依赖 ➔ `daily_sentence_model.dart` / `daily_sentence_builtin.dart` / `settings_engine.dart`（读 `dailySentenceUseBuiltin`）
- 被消费 ➔ `home_page.dart`（展示、刷新、快捷批量新增）、`daily_sentence_controller.dart`、`daily_sentence_page.dart`（列表增/改/删/排序（上移/下移））、`daily_sentence_edit_page.dart`（新增/编辑，批量新增走 `addSentencesBatch`）

### `lib/features/shell/ui/daily_sentence_page.dart`（每日一句列表页）
- **功能**：用户自定义每日一句管理；**【2026-07-14 二次改版·按截图】** 自上而下：① 顶部「启用内置每日一句」开关卡（带图标 + 描述 + `CupertinoSwitch` 绑定 `SettingsController.dailySentenceUseBuiltin`）；②「今天可能会看到」预览卡（监听 `DailySentenceService.displaySentenceNotifier`，右侧刷新按钮调用 `refreshDisplaySentence()`）；③「我的语句 (N)」标题；④ `ListView.builder` 列表项 = 引用图标 + 文案（点击进入 `daily_sentence_edit_page` 编辑）+ 三点按钮（弹出 `CupertinoActionSheet`：编辑 / 上移 / 下移 / 删除；删除走 `CupertinoAlertDialog` 二次确认）；⑤ 底部描边「添加新的语句」按钮；⑥ 底部提示「点击「···」可上移 / 下移排序」（`long_press_reorder`）。列表项以 `ValueKey(item.id)` 作为唯一 key。⚠️ **架构坑（2026-07-14 四次修改·删除崩溃彻底修复）**：本工程为纯 Cupertino（`CupertinoApp` 根），此前列表用 Material 的 `ReorderableListView`，删除项导致 itemCount 变小时 `SliverReorderableList` 在重建帧断言崩溃（Flutter 已知问题）。修复：弃用 `ReorderableListView`，改用原生 `ListView.builder`（同步移除外层 `Localizations` Material 包裹层与 `ReorderableDragStartListener` 拖拽手柄，不再 import `flutter/material.dart`），删除 / 编辑重建均稳定；排序由「···」菜单的「上移 / 下移」经 `DailySentenceService.reorderSentence` 完成，功能不丢。
- ⚠️ **架构坑（2026-07-14 三次修改·删除崩溃初修，已被四次修改取代）**：曾尝试移除 `ReorderableListView.builder` 的 `shrinkWrap:true`（对照 `image_to_pdf_page.dart` 不带 `shrinkWrap` 可正常删除），但实测删除仍崩溃；最终方案为四次修改的「弃用 `ReorderableListView`、改用原生 `ListView.builder`」，参见上一行。
- 依赖/调用 ➔ `daily_sentence_controller.dart`（增/改/删，批量新增走 `addSentencesBatch`）/ `daily_sentence_service.dart`（排序、刷新、displaySentenceNotifier、批量新增）/ `localization_engine.dart`（新增 `batch_add_hint`）/ `settings_controller.dart` / `daily_sentence_edit_page.dart`（批量新增入口）

### `lib/features/shell/ui/daily_sentence_edit_page.dart`（每日一句新增/编辑页）
- **功能**：全屏新增/编辑单条每日一句；`widget.sentence == null` 为新增模式、`!= null` 为编辑模式。**【2026-07-14 三次修改·批量增加】** 新增模式下「保存」按回车/换行（`RegExp(r'[\r\n]+')`）拆分文本框为多行，过滤空行后调用 `DailySentenceController.addSentencesBatch` 逐条批量新增（一行一个每日一句）；编辑模式仍走 `updateSentence` 单条更新。新增模式下文本框下方显示 `batch_add_hint` 提示（本地化，非硬编码）。内容为空时弹 `CupertinoAlertDialog` 提示（复用 `enter_content`），不关闭页面。颜色走 `CupertinoColors`/`CupertinoTheme`、文案走 `LocalizationEngine`，无硬编码。
- 依赖/调用 ➔ `daily_sentence_controller.dart`（新增/编辑/`addSentencesBatch`）/ `localization_engine.dart` / `daily_sentence_model.dart`
- 被消费 ➔ `daily_sentence_page.dart`（`_navigateToEdit` 跳转）

### `lib/features/shell/controller/settings_controller.dart`（设置控制器）
- **功能**：全局设置状态暴露与主题色唯一上浮入口。**【2026-07-14 本次修改】** 新增 `dailySentenceUseBuiltin`（`ValueNotifier<bool>`）与 `setDailySentenceUseBuiltin(bool)`。**【2026-07-14 自定义配色】** 新增 `activePrimaryColor`（`ValueNotifier<Color>`，全局生效主色，`shell_page` 据此重建主题）、`activeCustomColorId`（`ValueNotifier<String?>`，当前选中自定义色 id）、`customColors`（桥接 `CustomThemeColorService.colorsNotifier`）；新增 `resolveThemeColor(key)`（统一预设色解析，消除 shell/appearance/profile 三处重复 if-else）、`setPresetColor(key)`（预设选中并上浮主色）、`applyCustomColorById(id)`、`addCustomColor(color,{name})`、`updateCustomColor(id,color,{name})`、`deleteCustomColor(id)`；保留 `setThemeColor` 作为 `setPresetColor` 兼容包装。
- 依赖/调用 ➔ `settings_engine.dart`（读/写预设色 Key、自定义色哨兵 `themeColorCustom`）/ `custom_theme_color_model.dart` / `custom_theme_color_service.dart`（自定义色 CRUD 与 notifier）
- 被消费 ➔ 全局 UI（本次 `home_page.dart`、`daily_sentence_page.dart`、`appearance_page.dart`（主题配色 + 自定义配色）、`shell_page.dart` 监听 `activePrimaryColor`）

### `lib/engine/settings_engine.dart`（设置引擎）
- **功能**：全局设置 Key 与存取；**【2026-07-14 本次修改】** 新增 `dailySentenceUseBuiltinKey`（`app.dailySentence.useBuiltin`）、`dailySentenceUseBuiltinDefault`（true）、`dailySentenceUseBuiltin` getter/setter（经 `Config`）。**【2026-07-14 自定义配色】** 新增 `themeColorCustom`（`'custom'`，选中自定义配色时的 `themeColor` 哨兵值，使预设高亮让位于自定义选中态）。
- 被消费 ➔ `settings_controller.dart`（`resolveThemeColor`/`setPresetColor`/`themeColorCustom`）/ `daily_sentence_service.dart`

### `lib/engine/localization_engine.dart`（本地化引擎 · 全局唯一文案来源）
- **功能**：集中式多语言，对外暴露 `LocalizationEngine.text('key')`；含 zh/en 双语。
- **【2026-07-14 本次修改】** 新增键：`daily_sentence_refresh`(换一句) / `daily_sentence_use_builtin`(启用内置每日一句) / `daily_sentence_use_builtin_desc`(关闭后只显示你自定义的每日一句) / `daily_sentence_empty_custom`(无自定义句时的占位文案)。**【2026-07-14 二次修改】** 列表页改版新增：`my_sentences`(我的语句) / `today_preview`(今天可能会看到) / `refresh_one`(换一个) / `add_new_sentence`(添加新的语句) / `long_press_reorder`(长按可拖动排序) / `sentence_delete_confirm`(确认删除此句？) / `sentence_deleted`(已删除) / `move_up`(上移) / `move_down`(下移) / `batch_add_hint`(支持批量添加：每行一句，按回车换行可一次添加多条)。
- **【2026-07-14 转换页统一改版新增】** 转换类工具页共享文案（供 `conversion_scaffold.dart` 与 5 个转换页复用）：`conv_tab_convert`(转换) / `conv_tab_records`(记录) / `conv_select_file` / `conv_select_txt` / `conv_select_doc` / `conv_select_ppt` / `conv_select_excel` / `conv_select_images` / `conv_start`(开始转换) / `conv_converting`(转换中) / `conv_selected_file`(已选文件) / `conv_open`(打开) / `conv_view`(查看) / `conv_add_shelf`(加入书架) / `conv_added_shelf`(已加入书架) / `conv_no_record`(暂无转换记录) / `conv_delete`(删除) / `conv_delete_confirm_title` / `conv_delete_confirm_msg` / `conv_cancel`(取消) / `conv_open_failed`(打开失败) / `conv_file_not_found`(文件不存在) / `conv_tip_txt` / `conv_tip_doc` / `conv_tip_ppt` / `conv_tip_excel` / `conv_tip_image` / `conv_selected_count`(已选 %d 张，含占位符) / `conv_image_count`(%d 张图片) / `conv_convert_failed`(转换失败)。均含 zh/en 双语。
- 主要相关键（节选）：`bookshelf` / `bookshelf_all_label`(全部) / `bookshelf_tab_all`(全部) / `bookshelf_tab_other` / `bookshelf_favorites_label`(收藏) / `bookshelf_reading_label`(在读) / `bookshelf_finished_label`(已读) / `done` / `view_all`；回忆页与统计页全套键（`reading_statistics` / `stats_tab_week|month|year|all` / `last_year_today` / `random_memory` / `weekly_reading_duration` / `reading_timeline` / `heatmap_*` / `forgotten_*` / `all_reading_records` / `timeline_*` / `weekly_compare_prefix` 等）；每日一句键（`daily_sentence` / `enter_content` / `no_sentences` / `view_full` / `daily_sentence_refresh` / `daily_sentence_use_builtin` / `daily_sentence_use_builtin_desc` / `daily_sentence_empty_custom` / `my_sentences` / `today_preview` / `refresh_one` / `add_new_sentence` / `long_press_reorder` / `sentence_delete_confirm` / `sentence_deleted` / `move_up` / `move_down`）。
  - **【2026-07-14 日视图修正】** 新增 `hour_unit`(时/h)；`heatmap_day_block_hint` 文案改为描述「4 行×6 列小时格」布局（原『每小时切 4 格』）。
- **【2026-07-14 自定义配色新增】** 自定义配色键：`custom_theme_color`(自定义配色) / `custom_color_pick`(选择颜色) / `custom_color_palette`(调色板) / `custom_color_name`(名称) / `custom_color_add`(添加配色) / `custom_color_delete`(删除配色) / `custom_color_delete_confirm`(确认删除该配色？) / `custom_color_section_hint`(点击色块应用，长按可编辑或删除) / `custom_color_membership_hint`(自定义配色为会员功能，开通会员后可创建专属配色)。均含 zh/en 双语。
- 被全局所有 UI 调用。

### `lib/features/shell/ui/about_page.dart` + `lib/shared/ui/about_page_header.dart`（关于页）
- **功能**：`AboutPageHeader` 自定义组件（3D 书插图、浮动图标、标题/副标题/版本）；`about_page.dart` 用其替换原描述框。
- 依赖 ➔ `app_text_styles.dart` / `localization_engine.dart`（新增 `about_app_mission`）

### `lib/features/shell/ui/tools_page.dart`（工具页）
- **功能**：工具页，以「分类标题 + 响应式网格」展示 5 个转换工具。数据驱动：`_ToolItem`（categoryKey / icon / titleKey / subtitleKey / page）列表，按 `categoryKey` 运行时自动分组（电子书转换 / 转 PDF）；网格列数随屏宽响应式（手机 2 列、平板 3~4 列）。
- 卡片为纯展示（Dumb UI）：图标底 `primaryColor.withValues(alpha:0.12)`、背景 `secondarySystemBackground.resolveFrom(context)`、淡阴影 `primaryColor.withOpacity(0.06)` 取代边框；标题/副标题走 `AppTextStyles.body` / `secondary`，无硬编码字号与颜色。
- 全部文案走 `LocalizationEngine`：`tools`(工具) / `tools_cat_ebook`(电子书转换) / `tools_cat_pdf`(转 PDF) / `tool_{txt_epub,doc_pdf,ppt_pdf,xls_pdf,img_pdf}_title` + `_sub`。
- 依赖/调用 ➔ `lib/engine/localization_engine.dart`（全部文案）
- 依赖/调用 ➔ `lib/shared/ui/app_text_styles.dart`（`body` / `secondary` 语义文本样式）
- 跳转 ➔ `txt_to_epub_page.dart` / `doc_to_pdf_page.dart` / `ppt_to_pdf_page.dart` / `excel_to_pdf_page.dart` / `image_to_pdf_page.dart`（由 `_ToolItem.page` 注入）

### `lib/shared/ui/conversion_scaffold.dart`（转换页统一 UI 脚手架 · 新增共享组件）
- **功能**：为 5 个转换工具页提供统一的纯展示（Dumb UI）外壳与构件集合，保证 Android / iOS / iPad 观感一致。所有数据经参数传入、交互经回调抛出，无业务依赖。
- **常量/构件**：`kConvRadius=16`（圆角）、`kConvMaxContentWidth=560`（宽屏内容最大宽度，iPad/横屏居中不铺满）、`conversionCardDecoration`（次级系统背景 + 柔和阴影，跟随暗色）；`ConversionScaffold`（导航栏 + `IndexedStack` 内容区 + **底部** `CupertinoSlidingSegmentedControl` 转换/记录 Tab（贴近拇指、带顶部发丝分隔线、自身处理底部安全区），`LayoutBuilder` 宽屏居中）；`ConversionInfoCard`（提示/已选文件卡）；`ConversionPrimaryButton`（主按钮，支持 loading）；`ConversionEmptyState`（空记录占位）；`ConversionRecordCard` + `ConversionRecordActions`（记录卡与操作区：primary / successBadge / danger）；`ConversionFormat`（timestamp / dateTime / fileSize 格式化）。
- **公共方法**：`openConversionFile(context, filePath, {onMessage})` —— 校验 `File.exists()` 后调用 `OpenFilex.open`，失败弹 `CupertinoAlertDialog`（修复移动端「打开」假弹窗 BUG）；`confirmConversionDelete(context)` —— 删除二次确认弹窗，返回 `bool`。
- 颜色全走 `CupertinoColors` + `withValues(alpha:)`（已弃用 `withOpacity`），字号走 `AppTextStyles`，文案由调用方传入（已本地化）。
- 依赖/调用 ➔ `lib/engine/localization_engine.dart`（`conv_*` 文案）
- 依赖/调用 ➔ `lib/shared/ui/app_text_styles.dart`（语义字号）
- 依赖/调用 ➔ `package:open_filex`（真实打开导出文件）
- 被消费 ➔ `txt_to_epub_page.dart` / `doc_to_pdf_page.dart` / `ppt_to_pdf_page.dart` / `excel_to_pdf_page.dart` / `image_to_pdf_page.dart`

### `lib/features/{txt_to_epub,doc_to_pdf,ppt_to_pdf,excel_to_pdf}/ui/*.dart`（4 个单文件转换页 · 统一重写）
- **功能**：均重写为消费 `ConversionScaffold`，结构统一为「转换」Tab（提示卡 → 选择文件按钮 → 已选文件卡 → 开始转换按钮 → 状态文案）+「记录」Tab（空态或 `ConversionRecordCard` 列表，操作：打开 / 删除）。
- **修复**：① 移动端「打开」由假弹窗改为 `openConversionFile` 真实打开；② 新增删除记录能力（`confirmConversionDelete` 二次确认 → 控制器 `deleteExportRecord(timestamp)`）；③ 全部硬编码文案/字号/颜色迁移至 `LocalizationEngine` + `AppTextStyles` + `CupertinoColors`。
- 依赖/调用 ➔ `../../../shared/ui/conversion_scaffold.dart` / `localization_engine.dart` / `app_text_styles.dart`
- 依赖/调用 ➔ 各自 `controller`（select / convert / getExportRecords / **新增 deleteExportRecord**）

### `lib/features/image_to_pdf/ui/image_to_pdf_page.dart`（图片转 PDF 页 · 统一重写 + 拖拽排序）
- **功能**：消费 `ConversionScaffold`。「转换」Tab：提示卡 → 多选图片按钮 → 已选数量（`conv_selected_count`）→ 横向 `ReorderableListView.builder` 缩略图（角标 × 删除 + 拖动重排）→ 开始转换 → 状态；「记录」Tab：`ConversionRecordCard`（张数 · 文件大小 · 时间，操作：查看 / 加入书架 / 删除）。
- **修复**：① 兑现「拖拽重新排序」——`onReorder` 真正调用 `ImageToPdfController.reorderImages`（此前从未调用）；② 移动端「查看」走 `openConversionFile` 真实打开；③ 加入书架经 `BookshelfService().importPdf`。
- **【2026-07-14 转换修复·选图闪退】**：此前选择一个图片即闪退（崩溃 `No MaterialLocalizations found`）。根因：`ReorderableListView` 是 Material 组件，纯 `CupertinoApp` 树缺 Material 文案环境会立即断言崩（列表非空即触发）。修复：用 `Localizations(locale: const Locale('en'), delegates: [DefaultWidgetsLocalizations.delegate, DefaultMaterialLocalizations.delegate])` 仅包裹该列表（**不引入 `MaterialApp`**，避免嵌套导航冲突）；`buildDefaultDragHandles:false`，拖拽手柄改由 `ReorderableDragStartListener` + `CupertinoIcons.line_horizontal_3` 自绘，贴合 Cupertino 主题；并移除 `_onReorder` 里 `if (newIndex > oldIndex) newIndex -= 1;` 造成的二次错位 BUG（Cupertino 风格 `onReorder` 的 `newIndex` 已是删除旧项后的目标位，不应再减 1）。先例参照 `daily_sentence_page.dart` 的 `Localizations` 包裹手法。
- 依赖/调用 ➔ `package:flutter/material.dart show ReorderableListView, ReorderableDragStartListener, DefaultMaterialLocalizations, DefaultWidgetsLocalizations`（唯一 Material 引用）/ `conversion_scaffold.dart` / `localization_engine.dart` / `app_text_styles.dart`
- 依赖/调用 ➔ `controller/image_to_pdf_controller.dart`（reorderImages / getExportRecords / deleteExportRecord / addExportedPdfToShelf）
- 依赖/调用 ➔ `model/export_record_model.dart` / `../../shell/service/bookshelf_service.dart`

### `lib/shared/util/cjk_font_loader.dart`（CJK 字体字节加载器 · 新增【2026-07-14 转换修复】）
- **功能**：静态 `CjkFontLoader.loadBytes()` 从 `rootBundle` 加载内嵌中文字体 `assets/fonts/cjk.ttf`（SimHei，完整 CJK 字形），首次加载后以 `Uint8List?` 缓存复用；供 doc/ppt/excel/image 转 PDF 时注入 `pw.Font.ttf(bytes)`，解决中文空白/乱码。
- 资源来源：`pubspec.yaml` 的 `flutter.assets: - assets/fonts/cjk.ttf`（字体文件复制自系统 `C:\Windows\Fonts\simhei.ttf`，9.7MB，含完整中文字形）。
- 依赖 ➔ `package:flutter/services.dart`（`rootBundle`）
- 被消费 ➔ `doc_to_pdf_service.dart` / `ppt_to_pdf_service.dart` / `excel_to_pdf_service.dart` / `image_to_pdf_service.dart`（convert 时由主线程 `loadBytes()` 取字节，随 `compute` 的 `args` 传入 isolate；**注意 isolate 内无法访问 `rootBundle`，故字体字节须主线程加载后传递**）。

### `lib/features/{txt_to_epub,doc_to_pdf,ppt_to_pdf,excel_to_pdf}/service/*.dart` + `controller/*.dart`（新增删除能力 + 转换修复）
- **service**：各新增 `deleteExportRecord(int timestamp)` —— 读取记录 JSON、删除对应物理导出文件、`removeWhere(timestamp)` 后回写。
- **controller**：各新增静态 `deleteExportRecord(int timestamp)` 委托到对应 service，供 UI 调用（image_to_pdf 早已按 recordId 具备删除能力，本次未改其签名）。
- **【2026-07-14 转换修复】** 此前 5 个工具转换产物均不可用，统一修复如下：
  - `txt_to_epub_service.dart`：此前生成的 `.epub` 实为裸 XHTML 文本（任何阅读器打不开）。重写 `_buildEpubBytes` 用 `archive` 拼标准 EPUB（ZIP：`mimetype` 必须首文件且存储不压缩 `..compressionLevel=0` → `META-INF/container.xml` → `OEBPS/content.opf` → `OEBPS/toc.ncx` → `OEBPS/content.html`），可被阅读器正常打开；新增 `_decodeText` 编码探测：先 UTF-8（严格、拒绝替换符），失败/含 `�` 回退 `fast_gbk` 解码，解决中文 TXT（常见 GBK）乱码；正文 `_buildParagraphs` 按空行切片为 `<p>`，`_escapeXml` 转义 `< & >` 防 XML 破坏。仍走 `compute(_generateEpubAndSave, args)`，`args` 仅含路径/标题（字体不涉及）。
  - `doc_to_pdf_service.dart`：此前用 `pdf` 默认 `Helvetica` 缺 CJK 字形 → 中文空白/方块。注入内嵌 CJK 字体（`CjkFontLoader.loadBytes()` 取 `assets/fonts/cjk.ttf` 字节 → `pw.Font.ttf(bytes)`，`pw.Document(theme: pw.ThemeData.withFont(base: cjkFont, bold: cjkFont))`）；`_extractTextFromDocx` 改为正则提取 `<w:p>` 内的 `<w:t>` 文本并 `_decodeXmlEntities`，中文正常呈现。
  - `ppt_to_pdf_service.dart`：同样 CJK 字体注入；`_extractTextFromPptx` 提取 `<a:p>` 内的 `<a:t>` 文本，`_slideIndex` 按文件名序号排序页面。
  - `excel_to_pdf_service.dart`：同样 CJK 字体；**关键修复** `_extractTextFromXlsx` 解析 `xl/sharedStrings.xml` 为索引表，`t="s"` 单元格的 `<v>` 作为索引取真实文本（此前直接把索引当文本 → 满屏数字），并支持 `inlineStr` 与纯数值单元格；`_decodeXmlEntities` 解码实体。
  - `image_to_pdf_service.dart`：此前所有图片被强制拉伸到 A4 导致严重变形。改为 `_imageSize`（解析 PNG/JPEG 原始宽高）+ `_getPageFormatFromImage`（按真实比例设 `PdfPageFormat(w,h)`，`pw.Image(..., fit: BoxFit.fill)` 铺满），每页与图片同比例不变形；仍走 `compute(generatePdfAndSave, args)`。
- **依赖新增（就地补充）**：`cjk_font_loader.dart`（字体字节加载，doc/ppt/excel 转 PDF 共用）/ `fast_gbk`（GBK 解码，仅 `txt_to_epub`）/ `archive`（构造 EPUB ZIP，txt_to_epub + 测试）/ `pdf`（doc/ppt/excel/image 转 PDF）。`pubspec.yaml` 新增 `flutter.assets: - assets/fonts/cjk.ttf` 与依赖 `fast_gbk`（dev 另加 `image` 供测试造 PNG）。

### `lib/features/shell/ui/shell_page.dart`（App 根 · Tab 容器 + 启动屏引导）
- **功能**：`CupertinoApp` 根，应用全局主题/语言/字体；`home` 为 `ShellBoot`。
- `ShellBoot`（新增 StatefulWidget）：承载底层 `CupertinoTabScaffold`（5 个 Tab：主页/书架/回忆/工具/我的）；在 `initState` 首帧后，按 `SettingsEngine.startupSplashType` 判断：非「不显示」则 `Navigator.push(SplashScreen)` 压入启动屏；启动屏关闭（pop）后露出已按 `SettingsEngine.startupPage` 定位的 Tab 容器（`ShellController` 构造时读取）。
- 依赖/调用 ➔ `splash_screen.dart`（启动屏）/ `settings_engine.dart`（读 startupSplashType）/ `settings_controller.dart`（appearance/`activePrimaryColor`/fontFamily/language 驱动主题与标签标题；`activePrimaryColor` 取代原 `themeColor` 字符串解析，支持任意预设或自定义 `Color`）/ `shell_controller.dart`（selectedIndex 初始标签）/ `bookshelf_page.dart`·`home_page.dart`·`memory_main_page.dart`·`tools_page.dart`·`profile_page.dart`（5 个 Tab 页）
- **【2026-07-14 三次修改】**：`main()` 在 `AppStatsService.initialize()` 之后新增 `await ReadingSessionService.initialize()`（加载 `reading_sessions.json`）。
- 被调用 ➔ `lib/main.dart`（`runApp(const ShellPage())`）

### `lib/features/shell/ui/splash_screen.dart`（实际启动屏 · 新增）
- **功能**：真正消费「启动页设置」的启动屏（此前缺失，导致所有启动页配置永不生效）。依据 `SettingsEngine` 配置渲染：
  - `startupSplashType`：不显示（不压栈/立即 pop）/ 文字（白字居中）/ 图片（背景图，本地文件 `FileImage` 或网络 `NetworkImage`，加载失败回退主题渐变）；
  - `startupSplashText` / `startupSplashImagePath`：展示文字与背景图（叠加半透明黑层保证可读性）；
  - `startupSplashDuration`：>0 启动每秒倒计时，归零自动 `pop`；<=0 永久等待手动进入；
  - `startupSplashEntryMode`：自动（仅「跳过」按钮可点）/ 点击（整屏可点立即进入）。
  - 完成后 `Navigator.pop()` 露出底层 Tab 容器（已按 `startupPage` 定位）。
  - 颜色全走主题（`CupertinoTheme.primaryColor` 派生深色渐变、`CupertinoColors` 系统色），无硬编码。
- 依赖/调用 ➔ `settings_engine.dart`（读取全部启动配置）/ `localization_engine.dart`（skip/enter 等文案）/ `app_text_styles.dart`
- 被压入 ➔ `shell_page.dart`（`ShellBoot` 首帧 `Navigator.push`）

### `lib/features/shell/ui/splash_settings_page.dart`（启动页设置页 · 接线实际功能）
- **功能**：此前仅配置了 UI，本次接通实际逻辑（符合提示词「完善实际功能」）：
  - 内容类型（不显示/文字/图片）→ `SettingsController.setStartupSplashType`；
  - **图片卡片可点击**：`FilePicker` 选单图（已申请 `permission_handler` 相册/存储权限），落库 `setStartupSplashImagePath`，卡片内实时显示缩略图与文件名，无图时提示「尚未选择图片」；
  - **文字卡片可点击**：`CupertinoAlertDialog` + `CupertinoTextField` 编辑，`setStartupSplashText` 落库，卡片显示当前文字；
  - 显示时长（1/3/5/永久）→ `setStartupSplashDuration`；
  - **进入方式（自动/点击）**：绑定 `SettingsController.startupSplashEntryMode` 真实状态（此前硬编码 `selected`），点击 `setStartupSplashEntryMode`；
  - **启动后跳转页面**：行可点击，弹 `CupertinoActionSheet` 选 `none/home/bookshelf/memory/tools/profile`，`setStartupPage`；
  - **预览卡反映真实配置**：不显示→占位提示；文字/图片→对应渲染（此前硬编码标题文案与渐变 hex）；
  - 清除硬编码字号与渐变 hex，改用 `AppTextStyles` 与主题派生色。
- 依赖/调用 ➔ `settings_controller.dart` / `settings_engine.dart` / `localization_engine.dart` / `app_text_styles.dart` / `package:file_picker` / `package:permission_handler`
- 入口 ➔ `appearance_page.dart`（「启动页设置」行 push 进入）

---

### `lib/features/shell/model/custom_theme_color_model.dart`（自定义主题色模型 · 新增【2026-07-14 自定义配色】）
- **功能**：用户自定义配色实体（`id` 唯一标识 / `colorValue` 32 位 ARGB 整型便于 JSON 持久化 / `name` 可选名称）。`color` getter 由整型还原 `Color`；`copyWith` 局部更新；`toJson`/`fromJson` 序列化。
- 依赖：仅 `dart:ui`（`Color`），无业务依赖。
- 被消费 ➔ `custom_theme_color_service.dart` / `settings_controller.dart` / `appearance_page.dart`

### `lib/features/shell/service/custom_theme_color_service.dart`（自定义主题色持久化服务 · 新增【2026-07-14 自定义配色】）
- **功能**：维护用户自定义配色列表，本地 JSON（`custom_theme_colors.json`，应用文档目录，`path_provider`）持久化；`colorsNotifier`（`ValueNotifier<List<CustomThemeColor>>`）供 UI 监听；`addColor`/`updateColor`/`deleteColor`/`findById` 静态方法。遵循「服务层落盘、UI 仅监听 notifier」铁律，UI 不直接写持久化。
- 依赖 ➔ `custom_theme_color_model.dart` / `path_provider` / `dart:convert`
- 被消费 ➔ `settings_controller.dart`（桥接为 `customColors` notifier 与 CRUD 委托）/ `main.dart`（initialize 加载）

### `lib/features/shell/ui/custom_color_picker_sheet.dart`（取色弹层 · 新增 · Dumb UI【2026-07-14 自定义配色】）
- **功能**：纯展示取色弹层（`showCustomColorPicker`），含实时预览 + 名称输入 + 调色板（16 色）+ RGB 三滑杆；确认经 `onConfirm(color, name)` 回调上抛，内部不写持久化。颜色走 `CupertinoColors`/`CupertinoTheme`、文案走 `LocalizationEngine`、字号走 `AppTextStyles`，无硬编码色值/字号。
- 依赖/调用 ➔ `localization_engine.dart` / `app_text_styles.dart`
- 被消费 ➔ `appearance_page.dart`（`_CustomColorSection` 新增/编辑配色时弹出）

### `lib/features/shell/ui/profile_page.dart`（我的页面 · 改写【2026-07-14 自定义配色】）
- **功能**：「我的」页。账号卡（开通会员/同步）+ 快捷入口（每日一句/AI 助手/语言/**外观**/更多设置/关于）。【2026-07-14 二次调整】「主题配色 / 自定义配色」区块**不再内联于此页**，已整体迁移至 `appearance_page.dart`；本页仅保留「外观」入口（`app_appearance`）push 进入 `AppearancePage`，不再直接依赖 `settings_controller` / `permission_engine` / `custom_theme_color_model` / `custom_color_picker_sheet`。
- 依赖/调用 ➔ `localization_engine.dart` / `app_text_styles.dart` / `membership_page.dart`（账号卡「开通会员」跳转）/ `appearance_page.dart`（`app_appearance` 入口）
- 被消费 ➔ `shell_page.dart`（5 个 Tab 之一）

### `lib/features/shell/ui/appearance_page.dart`（外观设置页 · 改写【2026-07-14 自定义配色】）
- **功能**：应用外观（模式/预设配色 + 自定义配色/启动页）。预设色选择改用 `SettingsController.setPresetColor`（同步上浮 `activePrimaryColor`），解析复用 `SettingsController.resolveThemeColor`；**【2026-07-14 二次调整】** 新增「自定义配色」区块（`_CustomColorSection`，由 `profile_page` 迁入）：自定义配色 `Wrap` 网格、色块点按应用（`applyCustomColorById`）、长按弹 ActionSheet 编辑/删除（`updateCustomColor`/`deleteCustomColor`），受会员权限 `theme.customColor` 控制（未开通时添加框显示锁形并跳会员页）。预设行选中态联动 `activeCustomColorId`（自定义色激活时预设取消高亮）。
- **【2026-07-14 本次修改·配色格式统一】**：将 `_ThemeColorTile`、`_CustomColorSwatch`、`_CustomColorAddTile` 三处合为**唯一** `_ColorTile`，主题预设色与自定义配色**完全共用同一组件**——同样的框（`secondarySystemBackground` + 圆角 16 + 选中态主色描边）、同样的 28px 圆点展示配色、命名同样显示在下方（`AppTextStyles.secondary` fontSize 12），尺寸同样（预设行 6 列 Expanded、自定义网格也改为 **6 列间距 8**，两者单格等宽）。自定义色不再整块铺满配色、不再用内部勾选，改为与预设一致的「框 + 圆点 + 命名」。末尾「添加」占位同样用 `_ColorTile`（图标替代圆点、标注 `custom_color_add`）。
- 依赖/调用 ➔ `settings_controller.dart`（`activePrimaryColor`/`themeColor`/`activeCustomColorId`/`customColors`/`setPresetColor`/`applyCustomColorById`/`addCustomColor`/`updateCustomColor`/`deleteCustomColor`）/ `permission_engine.dart`（`hasPermission('theme.customColor')`）/ `settings_engine.dart` / `localization_engine.dart`（含新增 `custom_color_add`）/ `app_text_styles.dart` / `custom_color_picker_sheet.dart`（`_CustomColorSection` 取色弹层）/ `custom_theme_color_model.dart` / `membership_page.dart`（未开通会员时跳转）/ `splash_settings_page.dart`

### `lib/main.dart`（启动入口 · 改写【2026-07-14 自定义配色】）
- **功能**：应用启动。`WidgetsFlutterBinding` 初始化后依次 `AppStatsService.initialize` → `ReadingSessionService.initialize` → **新增** `CustomThemeColorService.initialize()`（加载自定义配色）；权限种子 JSON 新增 `"theme.customColor": true`（默认开放自定义配色，后续可由服务端下发关闭非会员入口）；随后 `ShellRegister.register()` 与 `runApp(ShellPage())`。
- 依赖 ➔ `permission_engine.dart` / `app_stats_service.dart` / `reading_session_service.dart` / `custom_theme_color_service.dart` / `shell_page.dart`

## 三、本次修改（2026-07-14）依赖变动摘要
- 涉及 UI 层（`home_page.dart` 卡片/每日一句样式统一 + 刷新按钮 + 联动；`daily_sentence_page.dart` 新增开关设置卡、**二次改版按截图重写为：开关卡 + 今日预览卡 + 可拖拽/编辑/删除的语句列表**）、数据层（新增 `daily_sentence_builtin.dart`）、服务层（`daily_sentence_service.dart` 新增取句与展示 notifier、**二次新增 deleteSentence/reorderSentence 静态方法、_saveSentences 改 static**）、配置层（`settings_engine.dart` + `settings_controller.dart` 新增 `dailySentenceUseBuiltin`）、文案层（`localization_engine.dart` 新增 4 键、**二次新增 9 个列表页键**）。
- **新增 Config Key**：`app.dailySentence.useBuiltin`（bool，默认 true），经 `Config` 持久化，由 `SettingsEngine` 封装、`SettingsController` 暴露 notifier 驱动 UI。无新增 Permission Key。
- **依赖方向**：`home_page` / `daily_sentence_page` → `settings_controller` → `settings_engine` → `Config`；`daily_sentence_service` → `settings_engine`(读开关) + `daily_sentence_builtin`(取句) + `daily_sentence_model`；UI 仅监听 notifier，不直接写持久化（符合禁区 3）。`daily_sentence_page` 通过 `ReorderableListView` + `DailySentenceService.reorderSentence` 实现长按拖拽排序；通过 `DailySentenceService.deleteSentence` 删除（二次确认）。**⚠️ 因 `ReorderableListView` 为 Material 组件，纯 Cupertino 树中须以 `Localizations(DefaultWidgetsLocalizations+DefaultMaterialLocalizations)` 包裹提供 `MaterialLocalizations` 环境，否则构建即崩。**
- **未触碰** `packages/`、未新增硬编码颜色/字号/文案（颜色走 `CupertinoColors`/`Theme`、文案走 `LocalizationEngine`）。
- 修改/新增文件：`home_page.dart`、`daily_sentence_page.dart`、`daily_sentence_service.dart`、`daily_sentence_controller.dart`、`settings_engine.dart`、`settings_controller.dart`、`localization_engine.dart`、新增 `daily_sentence_builtin.dart`；**工具页改版**：`tools_page.dart` 由单列列表重构为「分类标题 + 响应式网格」，硬编码中文标题/副标题迁至 `localization_engine.dart`（新增 11 个工具键），引用 `app_text_styles.dart`，未改变依赖方向 / 权限 Key、未触碰 `packages/`。
- **转换页统一改版**：新增共享脚手架 `lib/shared/ui/conversion_scaffold.dart`；重写 5 个转换页 UI（`txt_to_epub` / `doc_to_pdf` / `ppt_to_pdf` / `excel_to_pdf` / `image_to_pdf`）统一为「转换/记录」双 Tab 结构；`localization_engine.dart` 新增 30+ 个 `conv_*` 文案键；4 个单文件 service/controller 各新增 `deleteExportRecord(timestamp)`；引入 `open_filex` 修复移动端「打开/查看」假弹窗 BUG，兑现图片页拖拽排序。**依赖方向合规**：转换页 UI → `conversion_scaffold`（纯展示）+ 各自 controller → service，UI 不直接触碰持久化；`conversion_scaffold` 仅依赖 `localization_engine` / `app_text_styles` / `open_filex`，不反向依赖任何 feature。未新增 Config/Permission Key，未触碰 `packages/`，未新增硬编码颜色/字号/文案。
- **完善启动设置实际功能**：此前 `splash_settings_page.dart` 仅配置了 UI、且真正消费配置的 `SplashScreen` 缺失，导致配置永不生效。本次：① 新增 `lib/features/shell/ui/splash_screen.dart`（实际启动屏，按 `SettingsEngine` 的 `startupSplashType`/`Text`/`ImagePath`/`Duration`/`EntryMode` 渲染，倒计时归零或点击进入后 `pop` 露出底层已按 `startupPage` 定位的 Tab 容器）；② 重构 `shell_page.dart`，抽出 `ShellBoot` 在首帧按需把 `SplashScreen` 压入导航栈（仅当类型非「不显示」）；③ 启动设置页接通真实交互：图片卡片 `FilePicker` 选图并实时预览、文字卡片弹窗编辑、进入方式绑定 `startupSplashEntryMode` 真实状态、跳转页弹 `CupertinoActionSheet` 选择；④ `settings_engine.dart` 新增 `startupSplashEntryMode`（auto/tap，默认 auto）及 getter/setter，`settings_controller.dart` 新增 `startupSplashEntryMode` notifier 与 setter；⑤ `localization_engine.dart` 新增 14 个启动屏/设置交互键；⑥ 清除预览卡与设置卡中既有的硬编码渐变 hex 与字号，改用 `AppTextStyles` 与主题派生色。**依赖方向合规**：`SplashScreen`/`SplashSettingsPage` → `settings_controller` → `settings_engine` → `Config`；`ShellBoot` → `SplashScreen`；UI 经 notifier 驱动、不直接写持久化。未触碰 `packages/`、未新增 Permission Key、未引入新第三方依赖（复用既有 `file_picker` / `permission_handler`）。
- **【2026-07-14 三次修改·删除崩溃修复 + 批量增加】** ① 修复「删除自定义每日一句崩溃」：`daily_sentence_page.dart` 的 `ReorderableListView.builder` 移除 `shrinkWrap:true`（删除项收缩时 `SliverReorderableList` 子项计数变化会断言崩溃；对照 `image_to_pdf_page.dart` 不带 `shrinkWrap` 可正常删除）；删除回调 `_confirmDelete` 包 `try/catch`；服务层 `daily_sentence_service.dart` 的 `deleteSentence`/`addSentence`/`addSentencesBatch`/`updateSentence` 统一改为「先更新内存 `sentencesNotifier` 再 `_saveSentences` 落盘 + try/catch」，落盘异常仅 `debugPrint` 不再向上抛出，彻底消除删除崩溃。② 新增批量增加：`daily_sentence_service.dart` 新增静态 `addSentencesBatch(List<String>)`（按行拆分、过滤空行、用「微秒时间戳 + 自增盐值 `_idSalt`」生成唯一 id、一次性落盘、返回新增条数）；`daily_sentence_controller.dart` 新增 `addSentencesBatch`；`daily_sentence_edit_page.dart` 新增模式按回车/换行拆分文本框调用批量新增并在文本框下方展示 `batch_add_hint` 提示（内容为空弹窗提示）；`home_page.dart` 的 `_quickAddDailySentence` 同样按回车拆分批量新增；`localization_engine.dart` 新增 `batch_add_hint` 键。**依赖方向合规**：均为 feature 内 `ui → controller → service → model`，UI 不直接写持久化、未触碰 `packages/`、未新增 Config/Permission Key、颜色走 `CupertinoColors`/`Theme`、文案走 `LocalizationEngine`。

### 阅读统计详情页（MemoryPage）：记录列表限长 + 区间导航箭头跨平台修复 + 日均阅读统计（2026-07-14）
- `lib/features/shell/ui/memory_page.dart`
  └─ 变更 ➔ 底部「阅读记录」列表限长，避免一直向下延伸：阅读明细会话 `sessions.take(5)` → `sessions.take(2)`；「读完了」书籍 `finished.take(3)` → `finished.take(2)`；其余记录经「查看全部」进入 `ReadingRecordsPage`。
  └─ 变更 ➔ 区间切换器两侧箭头 `_navArrow` 由 `GestureDetector` 改为原生 `CupertinoButton`（`padding: EdgeInsets.zero`，命中区由 `minSize` 默认 44 保证，圆形图标仍 32）。修复在 macOS 桌面端点击命中偶发不稳定的问题，并确保 iOS / Android / macOS 点击行为一致、触控目标 ≥44。
  └─ 新增 ➔ 额外统计行第 3 个指标卡「日均阅读」：`_buildExtraStatCards` 中按当前区间 `minutesBetween(start,end) / 区间天数(clamp 1)` 计算日均分钟，≥60 显示「X.X 时」否则「X 分」；图标 `CupertinoIcons.stopwatch`。
- `lib/engine/localization_engine.dart`
  └─ 新增 ➔ 多语言键 `daily_avg_reading_label`（zh='日均阅读' / en='Daily Avg'），供「日均阅读」指标卡使用，消除硬编码字面量。
- 规范依据 ➔ 提示词.md「[❌ 绝对禁区 4：硬编码 UI 值]」：新增文案走 `LocalizationEngine.text(...)`，指标卡复用既有 `_MetricTile` 主题派生取色。
- 验证 ➔ `flutter analyze` 两文件零 error（仅既有 `equal_keys_in_map` / `unused_element_parameter` / `withOpacity` 弃用等良性提示）。

---

## 三、本次修改（2026-07-14 PDF 阅读器视觉增强）依赖变动摘要

> 需求：PDF 设置页「翻页方式」移入「外观」置于亮度下方；设置页限高屏幕 3/4 且可滚动；新增「布局」模式（单页 / 双页 / 单页连续 / 双页连续）；「更多」新增「自动裁切」开关；新增「背景调节」（仅 PDF 扫描件）含对比度 / 饱和度滑块、去除颜色（黑白灰）开关、智能去杂色开关；并完成跨端布局与性能优化。

### 新增文件

#### `lib/features/shell/model/pdf_reader_settings.dart`（PDF 阅读器视觉设置模型 · 新增）
- **功能**：不可变数据模型，聚合 `layoutMode`(0 单页/1 双页/2 单页连续/3 双页连续)、`autoCrop`、`brightness`(0.3~1.5)、`contrast`(0.5~2.0)、`saturation`(0~2.0)、`removeColor`、`denoise`，提供 `copyWith` 与 `isAdjusted/needsRerender` 判定。
- **依赖**：纯数据，无业务/UI 依赖（仅 dart 原生类型）。
- **被消费 ➔** `book_viewer_page.dart`（UI 保留设置字段，待叠加层接入）。

#### `lib/features/shell/service/pdf_render_service.dart`（PDF 渲染服务 · 重写 · 纯逻辑层 · **当前为主渲染链路核心**）
- **【2026-07-15 主线C 引擎迁移】** 原自定义渲染管线因 Windows 下 PDFium 挂死而回退至 pdfx（见本节历史记录）；本轮**删除 pdfx、改用同内核（PDFium）全平台引擎 pdfrx**，本服务被重写为 pdfrx 版并重新接入主路径。
- **功能**：① `renderPageImage(PdfDocument, pageNumber, {renderWidth, autoCrop, denoise, manualCropLeft/Right/Top/Bottom})` 将某页渲染为 `ui.Image`（支持**原生精确裁切**与**手动框选裁切**：`page.render(x,y,width,height,fullWidth,fullHeight)` 在目标分辨率只渲染内容子区域；手动裁切优先于自动裁切，四边任一边 >0 即生效）；**宽度对齐**：裁切去除左右边距（宽度占比 <1）时按比例放大有效渲染宽度 `effW = fullW/(crop.right−crop.left)`，使裁切内容显示时仍铺满原显示宽度，与相邻未裁切页在宽度上对齐、大小一致（解决「裁后页面变窄变小」）；② **自动裁切** `computeCropFractions`：先以 240px 宽白底探针图渲染，读 `PdfImage.pixels`（按 `format` 区分 RGBA/BGRA），扫非近白像素求内容包围盒（归一化 0~1），再交由 pdfrx 在分渲染时裁掉白边（区别于旧「整体缩放去白边」近似）；③ `buildColorMatrix(settings)` 将亮度/对比度/饱和度/**色温**/去色合成 `ColorFilter.matrix` 所需 20 长度矩阵（逻辑未动）；④ 单页 `ui.Image` 缓存 `_renderCache`（按 `sourceName:页码:宽:裁切:去杂色:手动裁切4值`，文档 `disposeDocument` 时统一 dispose 防 GPU 泄漏）；⑤ 文档级串行锁 `_lockFor`（避免 Windows 下并发 render 句柄竞争崩溃）。
- **依赖 ➔** `package:pdfrx`（PdfDocument/PdfPage/PdfImage/像素扫描）、`dart:ui as ui`（ui.Image/ui.PixelFormat/Rect）、`package:synchronized`、`pdf_reader_settings.dart`。
- **被消费 ➔** `lib/features/shell/ui/pdf_custom_view.dart`（`_PdfPageWidget._load()` 调 `renderPageImage`，滤镜叠加调 `buildColorMatrix`）。

#### `lib/features/shell/ui/pdf_page_tile.dart`（PDF 单页渲染瓦片 · **【2026-07-15 主线C 删除】**）
- **【删除原因】** 旧自定义渲染管线（基于 pdfx）的瓦片构件，含真实 `cropRect` 裁切逻辑但从未被实例化；引擎迁移至 pdfrx 后，其职能由 `pdf_custom_view.dart` 的 `_PdfPageWidget` + `PdfRenderService.renderPageImage` 取代。已删除，避免双 PDFium 引擎并存。
- **替换者 ➔** `lib/features/shell/ui/pdf_custom_view.dart`。

#### `lib/features/shell/ui/pdf_reader_view.dart`（PDF 多布局阅读视图 · **【2026-07-15 主线C 删除】**）
- **【删除原因】** 旧自定义渲染管线（基于 pdfx）的多布局视图，仅被已删的 `pdf_page_tile.dart` 引用；引擎迁移后由新建的 `pdf_custom_view.dart`（PdfCustomView，自建双栏 2-up / 连续滚动 / 横向吸附翻页 / 原生精确裁切）取代。已删除。
- **替换者 ➔** `lib/features/shell/ui/pdf_custom_view.dart`。

### 修改文件

#### `lib/features/shell/ui/book_viewer_page.dart`（PDF 阅读器 · **【2026-07-15 主线C】引擎迁移 pdfx → pdfrx + 自建 PdfCustomView**）
- **变更**：**【2026-07-15 主线C 引擎迁移】** 渲染核心由 pdfx 官方 `PdfView`+`PdfController` 替换为 **pdfrx 低层 API + 自建 `PdfCustomView`**（全平台 PDFium 内核，避免双引擎）。具体：
  ① 删除 `PdfController` 与 `_pageBuilder`（PhotoView 相关），保留 `_pdfDocument`（`PdfDocument`）；`_initializePdf` 改为 `document.dispose()` 替代 `close()`，移除 `PdfController(...)` 赋值；② `dispose` 改调 `PdfRenderService.disposeDocument(_pdfDocument!)`（统一释放 `ui.Image` 与文档句柄，防 GPU 泄漏）；③ `_syncProgress` 改用 `_pdfDocument!.pages.length`（替代原 `_pdfController!.pagesCount`）；④ 删除 `_handleError`；⑤ `_buildPdfView()` 直接返回 `PdfCustomView(document: _pdfDocument!, settings: _readerSettings, pageMode: _selectedPageMode, onPageChanged: _syncProgress)`；⑥ `Expanded` 守卫由 `_pdfController == null` 改为 `_pdfDocument == null`。
- **GPU 滤镜叠加（沿用）**：颜色调整（亮度/对比度/饱和度/去色）经 `PdfRenderService.buildColorMatrix` 合成矩阵、`ColorFiltered` 包裹；智能去杂色 `ImageFiltered`（`ImageFilter.blur` sigma 1.4）包裹——均由 `PdfCustomView` 内 `_PdfPageWidget` 施加，纯 GPU 合成、无原生调用、Windows 安全。
- **能力边界（已解决的）**：双栏 2-up（封面单页 + 其后对开页）、单页连续滚动、横向吸附翻页（PageView）、**原生平铺精确裁切**（pdfrx 子区域 render）均已由 `PdfCustomView` 真实实现。
- **依赖/调用 ➔** `package:pdfrx`（PdfDocument/PdfPage/PdfImage）/ `pdf_custom_view.dart`（PdfCustomView）/ `pdf_render_service.dart`（`renderPageImage` + `buildColorMatrix`）/ `reader_settings_sheet.dart` / `settings_engine.dart` / `settings_controller.dart` / `bookshelf_controller.dart` / `reading_session_service.dart` / `localization_engine.dart` / `dart:ui as ui`（ui.ImageFilter 等）。

#### `lib/features/shell/ui/reader_settings_sheet.dart`（阅读设置抽屉 · 改写）
- **变更**：① PDF 设置页「翻页方式」由「更多」移入「外观」并置于「亮度」下方；② 「外观」新增「布局」模式（4 选项芯片）；③ 设置面板整体限高屏幕 **3/4**；④ 「更多」新增「自动裁切」开关、「背景调节」分区（对比度/饱和度滑块 + 去除颜色开关 + 智能去杂色开关）；⑤ **【2026-07-15 新增】「更多」顶部新增「重排」动作行**（`_ActionRow`，文案 `pdf_reflow`/`pdf_reflow_desc`，经 `onReflow` 回调切换单栏连续阅读模式）；⑥ 新增构件 `_SwitchRow`/`_SliderRow`/`_ActionRow`/`_sectionTitle`。新增构造参数（`onReflow` 等）均带默认值，**兼容 txt 阅读器**。
- **依赖/调用 ➔** `localization_engine.dart` / `settings_engine.dart` / `settings_controller.dart` / `pdf_reader_settings`（仅类型）。

#### `lib/engine/localization_engine.dart`（本地化引擎 · 修改）
- **变更**：**【2026-07-15 新增】** 新增双语键 `pdf_reflow`(重排) / `pdf_reflow_desc`(单栏连续、页面撑满，适合手机阅读)，供「更多」中重排按钮使用。
- **被全局 UI 调用**。

#### `lib/engine/settings_engine.dart`（设置引擎 · 修改）
- **变更**：新增 7 个 Config Key 及 getter/setter：`readerLayoutMode`(int,0) / `pdfAutoCrop`(bool,false) / `pdfBgBrightness`(double,1.0) / `pdfBgContrast`(double,1.0) / `pdfBgSaturation`(double,1.0) / `pdfBgRemoveColor`(bool,false) / `pdfBgDenoise`(bool,false)，均经 `Config` 在内存中共享（**注：`Config` 为纯内存 `Map`，不落盘持久化**，故设置不会跨启动保留）。
- **被消费 ➔** `settings_controller.dart` / `book_viewer_page.dart`（初始化状态）。

#### `lib/features/shell/controller/settings_controller.dart`（设置控制器 · 修改）
- **变更**：新增 7 个 `ValueNotifier` 与对应 `setXxx`（readerLayoutMode / pdfAutoCrop / pdfBgBrightness / pdfBgContrast / pdfBgSaturation / pdfBgRemoveColor / pdfBgDenoise），对外上浮入口，UI 仅监听 notifier 不直接写持久化。
- **依赖 ➔** `settings_engine.dart`。**被消费 ➔** `reader_settings_sheet.dart` / `book_viewer_page.dart`。

#### `lib/engine/localization_engine.dart`（本地化引擎 · 修改）
- **变更**：新增 13 个双语键：`reader_layout`(布局) / `reader_layout_single`(单页) / `reader_layout_double`(双页) / `reader_layout_single_continuous`(单页连续) / `reader_layout_double_continuous`(双页连续) / `pdf_auto_crop`(自动裁切) / `pdf_auto_crop_desc`(去除页面四周空白边距) / `pdf_bg_adjust`(背景调节) / `pdf_bg_contrast`(对比度) / `pdf_bg_saturation`(色彩饱和度) / `pdf_bg_remove_color`(去除颜色) / `pdf_bg_remove_color_desc`(仅显示黑白灰) / `pdf_bg_denoise`(智能去杂色) / `pdf_bg_denoise_desc`(去除影响阅读的小黑点、杂点)。均含 zh/en。
- **被全局 UI 调用**。

### 依赖方向与规范
- **渲染链路（当前）**：`book_viewer_page` → `PdfView`（pdfx 官方）→ `PdfController` → `_pageBuilder`（`PhotoViewGalleryPageOptions`/`PdfPageImageProvider`）→ **pdfx 内部管理 getPage/render/close**。此路径在 Windows 下经实测稳定（commit `2856939`）。
- **【2026-07-15 回退原因】** 自定义渲染管线（`PdfReaderView`→`PdfPageTile`→`PdfRenderService`→手动 `page.render()`）在 Windows 下导致进程「未响应」挂起——表现为点开 PDF 书籍后窗口标题栏显示「(未响应)」，无任何报错信息，TXT 正常打开。经三轮修复（生命周期→并发锁→JPEG 格式替换 PNG）均未解决。根因推测为自定义原生调用序列与 Windows PDFium 的交互方式不兼容（具体崩溃点需后续用 WinDbg 等工具定位），故回退至已验证的官方路径。
- **待定文件**（保留未删除，待排查后叠加层接入）：`pdf_reader_view.dart` / `pdf_page_tile.dart` / `pdf_render_service.dart`。
- **设置链路**：`reader_settings_sheet` → `settings_controller` → `settings_engine` → `Config`；UI 经 notifier 驱动、不直接写持久化（符合禁区 3）。PDF 专属设置（布局/裁切/对比度等）UI 已保留，当前仅做状态记录，渲染效果待叠加层接入。
- **未触碰** `packages/`；**未新增**硬编码颜色/字号/文案。注：本阶段（2026-07-15 重排/OCR 基础）**新增**第三方依赖 `flutter_onnxruntime`（通用 ONNX 推理，全平台 dart:ffi）并将 `image` 由 dev 提升为主依赖；`synchronized` 仍在 pubspec 中但未被主路径导入。详见「四、」。
- **【2026-07-15 崩溃根因小结】** 打开 PDF 即崩（连背景都没出来）的**真正根因是渲染格式用了 PNG**：昨日自定义渲染管线将 `page.render` 设为 `PdfPageImageFormat.png`；而 pdfx 在 Windows 上其官方 `PdfView` 的 `_render` 默认使用 `format: jpeg, backgroundColor: '#ffffff'`——**PNG 经 pdfx 原生通道输出在 Windows/PDFium 下会直接崩溃**。`Config` 为纯内存、无持久化，故默认设置即可复现（与是否改过设置无关）。修复：渲染格式统一改为 **JPEG + 白色背景**（对齐 pdfx 官方路径）。此前两轮误判（「多瓦片并发交错→加文档锁」「同页二次取页 use-after-free」）均非根因，但所加的单次 getPage+render+close、文档级串行锁、`closeDocument`、`addPostFrameCallback` 首帧后加载、显式 `childSize` 属合理防御性加固，予以保留。

---

## 四、本次修改（2026-07-15 PDF 重排 / OCR 阶段0 基础）依赖变动摘要

> 需求：为 PDF 重排功能打基础。文字型 PDF 走纯 Dart 文本抽取；扫描件 PDF 走 OCR（用户已选定 PaddleOCR + onnxruntime 全平台方案）。阶段0 目标＝排雷：验证 `flutter_onnxruntime` 原生库在 Windows 可加载并跑通推理（不挂死），规避上次 PDFium 式「未响应」。

### 新增文件

#### `lib/features/shell/service/pdf_ocr_service.dart`（PDF OCR 服务 · 新增 · 纯逻辑层）
- **功能**：扫描件重排的数据来源。基于 `flutter_onnxruntime` 通用 ONNX 绑定，自行实现 PaddleOCR 检测/识别流水线。**阶段0**：仅含 `runAdditionSmokeTest()`（assets 加载加法模型并推理）与 `runInferenceFromBytes(Uint8List)`（字节→临时文件→`createSession` 推理，绕过 `flutter test` 未注册 path_provider 的限制）用于验证原生库；`recognizePage(Uint8List)` 为阶段3 真正 OCR 入口（当前返回空列表，待接 det/rec/dict 模型）。
- **依赖 ➔** `package:flutter_onnxruntime`（dart:ffi 原生推理）、`dart:io`（临时文件）、`package:flutter/services.dart`（rootBundle）。
- **被消费 ➔** 待阶段1/3 由 `book_viewer_page.dart` 调用（扫描件走 OCR、文字型走纯 Dart 文本抽取）。

#### `integration_test/pdf_ocr_test.dart`（onnxruntime 原生库运行时验证 · 新增）
- **功能**：在真实 Windows 应用嵌入（`IntegrationTestWidgetsFlutterBinding`）中运行，原生插件已注册，真正触发 `flutter_onnxruntime` 原生库加载与一次加法推理，断言输出 [11,22,33]。用于排除「进程未响应」挂死风险。
- **说明**：本环境的 `flutter test`（headless tester）**不注册任何原生插件**（path_provider / onnxruntime 均报 MissingPluginException），故改用 integration_test 验证。运行：`flutter test integration_test/pdf_ocr_test.dart -d windows`。
- **依赖 ➔** `integration_test`、`flutter_test`、`pdf_ocr_service.dart`。

#### `assets/models/`（新增资源目录）
- 当前含 `addition_model.onnx`（82B，阶段0 冒烟测试用极简加法模型，由 onnx Python 包生成）。
- **阶段3** 将放入：`det.onnx`（DB 文本检测）、`rec.onnx`（CRNN 识别）、`ppocr_dict.txt`（字符字典，PaddleOCR 中文 18k+ 字典）。

### 修改文件（pubspec.yaml）
- `dependencies` 新增 `flutter_onnxruntime: ^1.7.0`（解析为 1.8.2；pub.dev 标明 Android/iOS/Linux/macOS/web/Windows 全平台，dart:ffi，活跃维护，含 GPU EP）。
- `image` 由 `dev_dependencies` **提升为主依赖**（运行时位图处理：自动裁边、OCR 预处理），版本 `^4.8.0`。
- `flutter.assets` 新增 `- assets/models/`。
- `dev_dependencies` 新增 `integration_test: sdk: flutter`（原生插件运行时验证）。

### Windows 构建离线化（本地 vendor onnxruntime · 2026-07-15 补）
- **问题**：`flutter_onnxruntime` 的 `windows/CMakeLists.txt` 默认 `USE_SYSTEM_ONNXRUNTIME=ON`，普通机器无系统 onnxruntime → 回退到 **CMake 配置期从 GitHub 下载 `onnxruntime-win-x64-1.22.0.zip`**。构建机拉不到 GitHub（代理/网络限制）即构建失败 → Windows 无 exe、无窗口、无明显报错。表现为「加 OCR 后 Windows 起不来，Chrome(web) 正常」。
- **修复**：把 `onnxruntime-win-x64-1.22.0` 的 `include/`+`lib/`（已剔除 .pdb，约 13MB）vendor 到 `windows/third_party/onnxruntime/`，并在 `windows/CMakeLists.txt`：
  1. `include(flutter/generated_plugins.cmake)` **之前** `set(ONNXRUNTIME_ROOT_DIR ".../third_party/onnxruntime" CACHE PATH FORCE)` → 触发插件 system 分支 `find_library/find_path` 命中本地副本，**不再联网下载**；
  2. 插件 system 分支只链 `.lib` 不拷 `.dll`，故额外 `add_custom_command(TARGET bookreader POST_BUILD ... copy_if_different *.dll)` 把 `onnxruntime.dll`/`onnxruntime_providers_shared.dll` 拷到 exe 旁，避免运行时加载不到。
- **已 gitignore** `windows/third_party/onnxruntime/`（大二进制不入库；需时按上法重新下载 vendor 即可）。
- **影响**：仅 Windows 构建链；web/Android/iOS/macOS/Linux 不受影响。阶段3 接入真实 PaddleOCR 模型时沿用同一 vendor 目录。

### 依赖方向与规范
- **层级**：仅 `lib/features/shell/`（feature 层），**不碰 `packages/`**。
- **OCR 链路（阶段3 形态）**：`book_viewer_page`（判断无文本层→扫描件） → `pdf_ocr_service.recognizePage`（位图→onnxruntime 推理 det+rec+dict→阅读顺序文本） → 复用 `PdfReflowView`（与文字型同一重排视图）。UI 不直接写持久化（禁区3），颜色走主题、文案走 `LocalizationEngine`（禁区4/6）。
- **【2026-07-15 阶段0 结论】** onnxruntime 选用 `flutter_onnxruntime`（通用绑定，全平台）而非 `pp_ocr`(仅Win)/`paddle_ocr_flutter`(仅Android)/`flutter_paddle_ocr`(仅Android/iOS/Web)——因用户要求全平台，需自行实现 PaddleOCR 流水线。阶段0 已通过 integration_test 在 Windows 真实加载并跑通原生推理，确认不会像 PDFium 那样挂死；完整 OCR 流水线留待阶段3。
- **未新增**硬编码颜色/字号/文案。

---

## 五、PDF 设置 4 项缺陷修复（2026-07-15 下午）依赖变动摘要

> 需求：用户反馈 ① 翻页方式/布局设置无真实效果 ② 主题配色圈与阅读背景圈大小不一/未对齐 ③ 点「外观」时底部 5 导航按钮被设置内容遮挡 ④ 自动裁切/去杂色/重排未生效、扫描件未真正重排。

### 修改文件
#### `lib/engine/settings_engine.dart`（设置引擎 · 修改）
- **变更**：新增 Config Key `readerPageMode`(int,默认0：0 左右翻页 /1 上下滚动 /2 仿真 /3 无) 及 `readerPageMode` getter / `setReaderPageMode` setter，补齐此前「翻页方式」缺失的持久化入口。
- **被消费 ➔** `settings_controller.dart` / `book_viewer_page.dart`（初始化 `_selectedPageMode`）。

#### `lib/features/shell/controller/settings_controller.dart`（设置控制器 · 修改）
- **变更**：新增 `ValueNotifier<int> readerPageMode` 与 `setReaderPageMode`，作为翻页方式对外上浮入口（UI 不直接写持久化，符合禁区3）。
- **依赖 ➔** `settings_engine.dart`。**被消费 ➔** `book_viewer_page.dart`。

#### `lib/features/shell/ui/book_viewer_page.dart`（PDF 阅读器 · 修改）
- **变更**：① **翻页真实生效**——`_buildPdfView()` 由「写死 `Axis.vertical`」改为按 `_selectedPageMode` 推导 `scrollDirection`（0/2→横向，1/3→纵向），并按「连续布局 或 上下滚动」推导 `pageSnapping`；`PdfView` 加 `ValueKey(方向+吸附)` 保证切换轴向时干净重建；② `_selectedPageMode` 初始化自 `SettingsEngine.readerPageMode`，回调改为经 `SettingsController.setReaderPageMode` 落库；③ **自动裁切生效**——`_pageBuilder` 在 `_autoCrop` 开启时把 PhotoView `initialScale/minScale` 提为 `contained*1.12`，将四周白边推出视区（纯 PhotoView 缩放，无原生调用；真正逐页内容感知裁切仍属阶段3）；④ **去杂色增强**——`ImageFilter.blur` 由 sigma 0.7 提到 1.4；⑤ **重排增强**——`_reflow()` 一键组合「上下滚动+单页连续+自动裁切」。
- **依赖/调用 ➔** 同前，另经 `SettingsController.setReaderPageMode` / `SettingsEngine.readerPageMode`。

#### `lib/features/shell/ui/reader_settings_sheet.dart`（阅读设置抽屉 · 修改）
- **变更**：① **圆圈对齐**——「阅读背景」色圈由 直径28/标签宽46/字号9/间距6 统一为与「主题配色」一致的 **直径40/标签宽54/字号11/间距8**（PDF 路径 `_buildBackgroundOptions` 与非 PDF 路径内联两处同步）；② **底部导航固定**——PDF 设置面板结构由 `SingleChildScrollView>Column[...底部导航]` 改为 `Column[Flexible(SingleChildScrollView(设置内容)), 固定底部导航 Padding]`，底部 5 按钮移出滚动区，切换「外观/更多」展开内容时始终可见、不再被遮挡。
- **依赖/调用 ➔** 同前。

### 依赖方向与规范
- 未触碰 `packages/`；未新增硬编码颜色/字号/文案；`flutter analyze` 0 error/0 warning。
- **能力边界（如实告知）**：**【2026-07-15 主线C 已 overturn】** 本节前文所述「pdfx 官方 `PdfView` 不支持双栏 2-up / 自动裁切仅近似」等局限，已在主线C 引擎迁移至 **pdfrx** 后由自建 `PdfCustomView` 真实解决（双栏 2-up、连续滚动、横向吸附翻页、原生平铺精确裁切均已生效）。**仍受限于引擎/非本次范围者**：①「仿真」翻页无书页卷曲动画（pdfrx 不提供，自建 `PageView` 为横向整页翻页）；② **图片扫描件的文字级重排仍需 OCR**（`recognizePage` 当前为空壳，属阶段3 PaddleOCR 集成）；当前「重排」为版式层面（连续滚动+去白边+撑满宽度）。

---

## 六、PDF 阅读器引擎迁移 pdfx → pdfrx（2026-07-15 主线 C）

> 需求（来自主线 B 后用户追问）：pdfx 官方 `PdfView` 不支持双栏 2-up（布局「双页/双页连续」降级为单页/单页连续）、「仿真」翻页无书页卷曲动画、自动裁切只是「缩放去白边」近似而非逐页内容包围盒精确裁切。用户选择**全量迁移到 pdfrx**（同 PDFium 内核、全平台），实现：双栏 2-up、连续滚动、横向吸附翻页、原生精确裁切；并删除 pdfx 死代码，避免双 PDFium 引擎并存。

### 新增文件

#### `lib/features/shell/ui/pdf_custom_view.dart`（PDF 自建阅读视图 · 新增 · Dumb UI）
- **功能**：取代 pdfx `PdfView`，自建 4 态版面：
  - `_spreads`（List<List<int>> 对开页划分：封面单页 `[0]` + 其后两两成对）；
  - `_isHorizontal = pageMode∈{0,2}`；`_isContinuous = layoutMode∈{2,3}`；`_usePageView = !_isContinuous && _isHorizontal`（横向逐页吸附翻页用 `PageView`，其余连续用 `ListView`）；
  - 进度经 `NotificationListener<ScrollNotification>`（滚动手势结束）上报 `onPageChanged`；
  - 单页尺寸（`pageW`/`pageH`）由**外层 `LayoutBuilder`** 提供（屏幕确定尺寸），`PageView`/`ListView` 在滚动轴上约束为无限，故不可在单页分支直接用其内部 `constraints`，否则「上下滚动」模式会触发「无限高度」断言。
  - `_buildSpread`：双页 `Row` + `Expanded`（各占 `(pageW-12)/2`）+ 12px 间距拼对开页；**单页用 `SizedBox` 直接铺满，不再把 `Expanded` 误放进 `Center`**（Center 非 Flex，在 `ListView` 无限主轴上会断言/崩溃）；
  - `_PdfPageWidgetState`：`PdfRenderService.renderPageImage(...)` 取 `ui.Image` → `RawImage(image: _image, fit: BoxFit.contain)`；叠加 `ColorFiltered`(buildColorMatrix) + `ImageFiltered`(blur 1.4)。**【2026-07-15 三个阅读缺陷修复】移除原 `InteractiveViewer` 缩放手势层**：其 scale 手势识别器会拦截单指滑动，导致 `PageView`/`ListView` 收不到翻页/滚动手势（表现为「无法翻页」）；移除后翻页/滚动交由原生控件处理（缩放能力后续以「仅在已放大态启用」等不冲突方式重新接入）。
- **依赖 ➔** `package:pdfrx`、`pdf_render_service.dart`、`pdf_reader_settings.dart`、`dart:ui as ui`（ui.Image / ui.ImageFilter）。
- **被消费 ➔** `book_viewer_page.dart`（`_buildPdfView()` 返回 `PdfCustomView`）。

### 修改文件

#### `lib/features/shell/service/pdf_render_service.dart`（重写 · 见本节前文条目）
#### `lib/features/shell/ui/book_viewer_page.dart`（改造接入 · 见本节前文条目）
#### `lib/features/shell/service/bookshelf_service.dart`（封面缩略图改 pdfrx · 见本节前文条目）
#### `pubspec.yaml`
- `dependencies` 删除 `pdfx: ^2.9.2`，新增 `pdfrx: ^1.3.5`（与既有 `image`/`pdf`/`flutter_onnxruntime` 并存）；`flutter pub get` 成功。

### 删除文件
- `lib/features/shell/ui/pdf_reader_view.dart`（pdfx 死代码，仅被 pdf_page_tile 引用）
- `lib/features/shell/ui/pdf_page_tile.dart`（pdfx 死代码，含真实裁切逻辑但未被实例化）
- `test/pdf_fmt_probe_test.dart`（pdfx PNG/JPEG 崩溃调试探针，引用已删 pdfx，导致 analyze error）

### 依赖方向与规范
- **渲染链路（当前）**：`book_viewer_page` → `PdfCustomView`（自建版面/翻页/裁切/滤镜）→ `PdfRenderService.renderPageImage` → `pdfrx`（PdfDocument/PdfPage/PdfImage 原生子区域 render）；颜色矩阵 `buildColorMatrix` 仍由 `PdfRenderService` 提供（纯逻辑、未动）。封面缩略图链路：`bookshelf_service._generatePdfCover` → `pdfrx`（openFile→pages[0]→render→createImage→toByteData）。
- **架构铁律合规**：`pdf_render_service` 为纯逻辑层、不反向依赖 UI；`pdf_custom_view` 数据经构造参数传入（`document/settings/pageMode/onPageChanged`）、交互经回调上抛（Dumb UI）；UI 不直写持久化（禁区 3）；颜色走主题派生 + `ColorFiltered`（禁区 4/6）；文案走 `LocalizationEngine`（禁区 4）。未触碰 `packages/`。
- **Windows 构建注意**：pdfrx 使用符号链接，Windows 构建需开启「开发者模式」。
- **验证**：`flutter analyze` 全工程 **0 error**（仅遗留既有 `deprecated_member_use`/`no_leading_underscores_for_local_identifiers`/`unused_*` 等 info/warning，非本次引入、不影响构建）。
- **Config / Permission Key**：**无新增**（沿用主线 B 既有 7 个 PDF 设置 Key：`readerLayoutMode`/`pdfAutoCrop`/`pdfBgBrightness`/`pdfBgContrast`/`pdfBgSaturation`/`pdfBgRemoveColor`/`pdfBgDenoise` + `readerPageMode`）。引擎替换不改变任何设置项语义。
- **仍受限（如实告知）**：①「仿真」翻页无书页卷曲动画（pdfrx 不提供，自建 `PageView` 为横向整页翻页）；② 扫描件文字级重排需 OCR（`pdf_ocr_service.recognizePage` 当前空壳，属阶段3 PaddleOCR 集成），本轮仅版式重排。

---

## 七、macOS 原生构建配置与依赖（2026-07-15 修复「改 pdfrx 后 mac 无法启动」）

> 现象：`flutter run -d macos` 报两处信息——① 警告「All plugins found for macos are Swift Packages, but your project still has CocoaPods integration」；② 致命错误「The package product 'flutter-onnxruntime' requires minimum platform version 14.0 for the macOS platform, but this target supports 10.15」→ **BUILD FAILED**。
> 根因：① 早期 `pod install` 残留的 CocoaPods 集成（Podfile / Pods / xcconfig 的 `#include? "Pods/..."`）与 Flutter 当前的 SwiftPM 插件机制并存；② 引入 `flutter_onnxruntime`（OCR 阶段0，全平台 FFI）后，其 Swift Package 要求 macOS ≥ **14.0**，而 `Runner` 的 `MACOSX_DEPLOYMENT_TARGET` 仍为 **10.15**，导致 `FlutterGeneratedPluginSwiftPackage`（Flutter 自动生成的插件包）平台被锁在 10.15，SwiftPM 解析 onnxruntime 依赖时直接报错。

### 修复后的 macOS 原生构建文件清单与功能 / 依赖
- **`macos/Podfile`（已删除）**：原 CocoaPods 集成描述文件（`platform :osx, '10.15'`）。Flutter 3.24+ 对 macOS 插件改用 Swift Package Manager，已不再需要；删除后 `flutter pub get` 不再重新生成（全部原生插件均支持 SwiftPM）。
- **`macos/Pods/`（已删除）**：CocoaPods 依赖目录，`pod deintegrate` 后清空。
- **`macos/Flutter/Flutter-Debug.xcconfig` / `Flutter-Release.xcconfig`**：去掉首行 `#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.{debug,release}.xcconfig"`（CocoaPods 残留），仅保留 `#include "ephemeral/Flutter-Generated.xcconfig"`。
- **`macos/Runner.xcodeproj/project.pbxproj`**：三处 `MACOSX_DEPLOYMENT_TARGET = 10.15` → `14.0`（Debug / Profile / Release 三个 PBXProject 级构建配置）。这是解决「14.0 vs 10.15」错误的根本改动——Flutter 在 `flutter run/build` 时通过 `SwiftPackageManager.updateMinimumDeployment` 读取该值，把 `FlutterGeneratedPluginSwiftPackage/Package.swift` 的平台从默认 10.15 提升为 14.0，从而满足 onnxruntime 的最低要求。
- **`macos/Runner.xcworkspace/contents.xcworkspacedata`**：移除 `<FileRef location = "group:Pods/Pods.xcodeproj">`（CocoaPods 残留引用），仅保留 `Runner.xcodeproj`。
- **`macos/Flutter/ephemeral/Packages/FlutterGeneratedPluginSwiftPackage/Package.swift`**（自动生成，勿手改）：`platforms: [.macOS("10.15")]`，依赖 `file_picker` / `flutter_onnxruntime` / `pdfrx` / `url_launcher_macos` / `FlutterFramework`。构建时被 Flutter 自动改写为 `.macOS("14.0")`；`flutter pub get` 会按模板重置为 10.15（属正常，构建期再提升）。
- **`macos/Runner/Info.plist`**：`LSMinimumSystemVersion` 经 `$(MACOSX_DEPLOYMENT_TARGET)` 自动跟随 14.0。

### 依赖关系与约束
- `flutter_onnxruntime`（见 `lib/features/shell/service/pdf_ocr_service.dart`，OCR 阶段0 验证） ⇒ 要求宿主 `FlutterGeneratedPluginSwiftPackage` 平台 ≥ 14.0 ⇒ `Runner` 部署目标必须 ≥ 14.0（已在 `project.pbxproj` 锁定 14.0）。
- 原生构建链路：`flutter pub get` 生成 SwiftPM 插件包 → `flutter run/build` 经 `updateMinimumDeployment` 把包平台对齐到 `MACOSX_DEPLOYMENT_TARGET(14.0)` → Xcode(SwiftPM) 链接 4 个原生插件（file_picker / flutter_onnxruntime / pdfrx / url_launcher_macos）→ 产出可运行的 `bookreader.app`。
- **⚠️ 副作用（必读）**：App 最低支持系统已抬到 **macOS 14.0（Sonoma）**，这是引入 `flutter_onnxruntime` 的必然后果；低于 14.0 的 Mac 将无法安装/运行。若未来新增原生插件要求更高系统版本，需同步上调 `project.pbxproj` 的 `MACOSX_DEPLOYMENT_TARGET`。
- **未触碰 `lib/` 与 `packages/`**：本次仅改 `macos/` 原生构建配置，无任何 Dart 架构层改动，符合 `docs/提示词.md` 的包隔离与禁区规范。

---

## 八、PDF 阅读器体验修复（2026-07-15 晚 · 用户 10 项反馈）

> 本轮针对「pdfrx 引擎迁移后」用户实测反馈的 10 个问题统一修复：自动裁切失准、单击不翻页、双页间隙过大、翻页方式/动画分区、双页首末页强制单页、去杂色降清晰度、书架仅显 4 本、页面被裁切、饱和度不生效、重排改为真实文本重排。涉及文件均位于 `lib/features/shell/`，未触碰 `packages/` 与架构禁区。

### `lib/features/shell/ui/pdf_custom_view.dart`（自建 PDF 视图 · Dumb UI）
- **功能**：基于 pdfrx 低层 API 自建版面与渲染，承接 `PdfReaderSettings` 与 `pageMode`/`pageAnimation`/`dualScreen`。
- **历史修改（2026-07-15 晚）**：
  - 翻页方式语义扩展为 5 种：`0` 左右滑动 / `1` 上下滑动 / `2` 左右单击 / `3` 上下单击 / `4` 单击滚动（单击滚动＝点击翻页 + 滑动滚动均可翻页）。`_isHorizontal` 取 `pageMode∈{0,2}`；`_swipeEnabled` 取非纯单击（2/3）；`_tapFlip` 取 2/3/4；`_usePageView = !_isContinuous`。
  - 新增 `pageAnimation`（0 无动画 / 1 仿真），`_animateToSpread` 据此 `animateToPage`/`animateTo` 或 `jumpToPage`/`jumpTo`。
  - `_onTapFlip`：左右单击按 `dx<width/2` 判前后；上下单击按 `dy<height/2`；单击滚动点任意处下一页。
  - `_buildSpreads` 取消「封面单页 + 末页强制单页」，改为从首页起两两成对（`for (i=0; i<pageCount; i+=2)`）。
- **历史修改（2026-07-16 凌晨 · 更多设置重组）**：`_pageGap` 由 12 → **6**；双屏开关 `dualScreen` 经 `PdfReaderSettings` 上浮。
- **本次修改（2026-07-16 上午 · 9 项需求）**：
  - 页间间隙 `_pageGap` 由 6 → **2.0**（`static const double _pageGap = 2.0`，连续/双页模式间隙大幅缩小，页面紧密衔接）。
  - 扫描版 PDF 默认屏幕居中：单页版面（`layoutMode==0`）`_PdfPageWidget` 的 `FittedBox` 对齐由 `Alignment.topCenter` 改为 `Alignment.center`；其余版面保持顶部对齐。
  - 双页高度不一致修复：`_buildSpread` 双页 `Row` 改为 `IntrinsicHeight` + `CrossAxisAlignment.stretch`（`crossAxisAlignment: CrossAxisAlignment.stretch`），强制左右两页等高、较矮页垂直居中填充。
  - 连续模式自然高度：单页/双页连续（`layoutMode∈{2,3}`）按页面实际宽高比推导容器高度（`effectiveH = 页面宽 × 高/宽`），消除强制撑满视口导致的大片空白间隙，上下页面紧密相连对齐。
  - 双屏模式：`build()` 开头 `if (widget.settings.dualScreen) return _buildDualScreenView(context)`；`_buildDualScreenView()`（水平分栏 + 1px `systemGrey4` 分隔线）内含左右两个 `_DualScreenPane`，各持独立 `ScrollController`、独立 `_paneKeys`（`late final List<GlobalKey>`，每栏 `initState` 自生成 `GlobalKey` 列表）与独立 `_flatPages`（`late final List<int>`，`[for (final s in widget.spreads) ...s]` 把对开页展平，确保两栏都渲染全部页而非仅 `pages[0]`）；左栏主导进度上报、右栏不主导。**崩溃修复（2026-07-16 上午第二轮）**：原两栏共用外层同一 `_spreadKeys` 列表 → 重复 `GlobalKey` 断言 `Duplicate GlobalKey` 直接崩溃；改为每栏各自生成 key 列表后崩溃消失。
  - 奇偶页分开裁边：`_PdfPageWidget._load()` 中按 `settings.cropOddEvenMode` 计算 `effectiveAutoCrop`（`0`=统一全页 / `1`=仅奇数页 `autoCrop && isOddPage` / `2`=仅偶数页 `autoCrop && !isOddPage`，`isOddPage = (pageIndex+1) % 2 != 0`）；`didUpdateWidget` 监听 `cropOddEvenMode` 变化触发重载。
  - **【2026-07-16 上午第二轮·5 项反馈修复】手动裁边落地**：`_PdfPageWidget._load()` 新增 `useManual`（`settings.cropMode == 2 && 四边任一边 >0`），成立时 `autoCrop` 置 false 并向 `PdfRenderService.renderPageImage` 透传 `manualCropLeft/Right/Top/Bottom`（真正生效、覆盖自动裁切，修复「框选裁边无反应」）；`didUpdateWidget` 同时监听 `cropMode` 与四个 `manualCrop*` 字段变化触发重载（此前框选确认后页面不刷新即此因）。
  - **【2026-07-16 上午 · 5 新功能续改 · 目录/进度/笔记/横屏/搜索书签】**：`_PdfPageWidget` 状态类由私有 `_PdfCustomViewState` 提升为 **public `PdfCustomViewState`**（供 `book_viewer_page` 经 `_pdfViewKey.currentState` 驱动跳转）；新增 `_leftPaneKey`/`_rightPaneKey`（`GlobalKey<DualScreenPaneState>`）；新增 `_pageToSpread(int)`（页码→对开页 index）、`jumpToPage(int)`（双屏模式同时驱动左右两栏 `_leftPaneKey/_rightPaneKey.currentState.jumpToPage` + 上报 `_reportPage`；非双屏走 `_animateToSpread`）、`goPrevPage()`/`goNextPage()`；`_DualScreenPane` 状态类提升为 public `DualScreenPaneState`，新增 `jumpToPage(int)`（经 `Scrollable.ensureVisible(_paneKeys[idx].currentContext, alignment: 0)` 滚动到目标页）。
  - **【2026-07-16 下午 · 双屏崩溃修复】**：`DualScreenPaneState.build` 的 `ListView` 原直接作为 `Row`+`Expanded` 子项，垂直方向收到**无界高度** → 抛 `Vertical viewport was given unbounded height` 直接崩溃（点击双屏开关即崩）。改为用 `SizedBox(height: widget.paneHeight)` 包裹 `ListView`，给出有界高度后崩溃消失。
- **本次修改（2026-07-16 晚 · 翻页动画 7 种 + 连续缩放 + 进度回拖修复）**：
  - 翻页动画枚举由 0/1 扩至 **0~8**（`settings_engine.dart` `readerPageAnimation` 注释同步）：0 无动画 / 1 仿真 / 2 淡入淡出 / 3 叠加 / 4 跃动 / 5 旋转 / 6 旋转木马 / 7 模仿圆筒 / 8 反转。
  - 非连续逐页（PageView）路径新增 `_buildAnimatedSpread`：用 `AnimatedBuilder` 监听 `PageController.page`，按相对偏移 `position`（0=当前页，±1=相邻页）调用 `_transformSpread` 施加变换——2 透明度 / 3 位移覆盖 / 4 正弦弹跳 / 5 平面旋转 / 6 3D 旋转+缩放 / 7 强透视圆筒 / 8 半圈翻转（`Matrix4` + `setEntry(3,2,..)` 透视，横纵翻页按 `scrollDir` 选择旋转轴）。`_animateToSpread` 改为「仅 `pageAnimation==0` 瞬时跳转，其余均 `animateToPage`/`animateTo` 过渡」。
  - **连续模式缩放（需求#3）**：连续（ListView）路径整条列用单个 `InteractiveViewer` 包裹（`transformationController:_zoomController`、`scaleEnabled:true`、`panEnabled:_zoomScale>1`、`minScale:1`、`maxScale:4`、`boundaryMargin:infinity`），整列等比缩放、页面天然紧密相连；`_zoomController` 监听器 `_onZoomChanged` 仅在放大态开启平移，未放大时单指滑动回落 ListView 滚动/翻页，修复此前 InteractiveViewer 手势冲突而整体移除的问题。
  - **进度条回拖不跳转修复（需求#4）**：原连续模式 `_animateToSpread` 用 `clamped - _currentSpreadIndex()` 相对位移，而 `_currentSpreadIndex()` 连续模式恒为 0 → 从末尾往回拖偏移被 clamp 到最大而页面不跳转。改为新增 `_offsetForSpread(index)` 经对比目标页与列表视口的 `localToGlobal` 差叠加当前 `scrollController.offset` 计算绝对目标偏移，前/后向拖拽均正确落点（书签 `jumpToPage` 同步收益）。
- 依赖/调用 ➔ `pdf_render_service.dart`（`renderPageImage` / `buildColorMatrix` / `invalidatePageCache`）/ `pdf_reader_settings.dart` / `package:pdfrx` / `dart:ui` / `dart:math`
- 被注入 ➔ `book_viewer_page.dart`（`_buildPdfView()` 返回 `PdfCustomView(document, settings, pageMode, pageAnimation, onPageChanged)`）

### `lib/features/shell/service/pdf_render_service.dart`（纯逻辑层）
- **功能**：pdfrx 渲染为 `ui.Image`、原生子区域精确裁切、真实去杂色、颜色矩阵合成。
- **本次修改**：
  - **【根因修复·点开即被裁切/双排更严重】无裁切分支原 `page.render(width: fullW, height: fullH)` 漏传 `fullWidth/fullHeight`，pdfrx 在缺省时回退页面原生 pt 尺寸作 `fullWidth/fullHeight`，把整页渲染进更小的位图 → 右/下边缘溢出被静默裁掉；双页 `targetWidth` 更小、裁切更重。改为 `page.render(fullWidth: fullW, fullHeight: fullH)`（不传 x/y/width/height），使 `width ??= fullWidth`、`height ??= fullHeight`，位图尺寸 == 整页渲染尺寸，零裁切。兜底分支（包围盒退化成整页）同样改 `fullWidth/fullHeight`。
  - 自动裁切探针 `computeCropFractions`：原 `render(width: cropScanWidth, height: probeH)` 同样漏传 `fullWidth/fullHeight`，导致探针只扫到整页左上角、包围盒偏小（开自动裁切仍误切右侧）。改为 `render(fullWidth: cropScanWidth, fullHeight: probeH)`，探针为真正的全页探针；`cropScanWidth` 240→480→**1200（2026-07-16 第二轮·防切内容：低分辨率把细内容平均成白底导致包围盒丢内容）**、阈值 235→238、内容占比<0.3% 跳过、边距 1.5%→2%（`max(1,...)` 兜底）、clamp、内容填满整页回退全页。
  - 真实去杂色 `_denoiseImage`：3×3 邻域墨点判定（孤立墨点＝邻域墨点<2），用邻域背景平均色替换；经回调式 `ui.decodeImageFromPixels(... , (image)=>completer.complete(image))` 回写成 `ui.Image`（修正先前误用 Future 返回式 API 导致的编译错误）。
  - `renderPageImage` 新增 `bool denoise` 参数，缓存 key 含 `:$denoise`，去杂色后释放旧图。
  - **【2026-07-16 下午 · 色温致颜色消失根因修复】** `_saturation(double s)` 矩阵存在严重 bug：原实现三行（R/G/B 输出）共用同一组 `[r,g,b]` 值，导致即使 `s=1.0`（默认值）矩阵也不是单位阵——G/B 输出通道被映射到 R，色彩完全丢失。只要 `buildColorMatrix` 进入合成链（任意参数≠默认即触发，如调色温），`_saturation(1.0)` 即摧毁颜色。修复：每行独立计算对角元（R 行第 1 列、G 行第 2 列、B 行第 3 列为 `s`），保证 `s=1.0` 时输出恒等矩阵。
  - **【2026-07-16 上午 · 9 项需求·智能去白边改进】** `_scanContent` 优化：① 近白阈值 238→**242**（更积极排除近白背景）；② 四周间隙 0.5%→**1.5%**（至少 2px，`max(2, w*0.015)`）：0.5% 把内容笔画也切掉（用户反馈「裁剪过度 / 去多了」），1.5% 留安全余量；③ 新增「边缘收紧（edge tightening）」：初步包围盒基础上从四边逐行列向内扫描，收缩到首个有内容像素（`r/g/b` 不全 >242）的行列，紧贴内容边缘；④ 内容占比<0.3% 视为空页不裁切；结果严格 clamp 不越界；探针 `cropScanWidth` 已在上一轮提到 480。新增静态 `invalidatePageCache(PdfDocument, int)`（按页清除 `_pageCache`，框选裁边确认后调用以强制重渲染）。
- 依赖 ➔ `package:pdfrx` / `dart:ui` / `dart:async` / `dart:typed_data` / `dart:math` / `synchronized`

### `lib/features/shell/ui/reader_settings_sheet.dart`（设置面板）
- **功能**：主题/背景/翻页/布局/裁切/滤镜统一面板；新增翻页动画与重排排版调节；双分支（PDF / 非 PDF）。
- **本次修改**：
  - 新增 `selectedPageAnimation`/`onPageAnimationChanged`（默认 1）与 `showReflow` 参数。
  - 新增 `_buildPageTurnSection`：翻页方式 5 模式单行（`reader_page_turn_swipe_h/v`、`reader_page_turn_tap_h/v`、`reader_page_turn_tap_scroll`），其下独立「翻页动画」分区（`reader_page_animation_none`/`reader_page_animation_simulation`）。
  - 新增 `_buildReflowTypographySection` + `_reflowSlider`：重排排版 4 项滑块（字体大小/行距/字距/段距），实时经 `SettingsController` 落库并广播（仅 `showReflow` 时显示）。
  - 替换原 4 模式旧文案（旧 `reader_page_turn_horizontal/vertical/simulation/none` 键已删除）。
- **本次修改（2026-07-16 上午 · 9 项需求）**：
  - 重排由 `_ActionRow`（仅进入）改为 `_SwitchRow`（能开能关）：开 → 进入真实文本重排（`_reflow()`），关 → 退出重排回版式（`onReflow` 回调在 `_isReflowing` 时退出）。
  - 外观设置中主题配色 / 阅读背景圆圈尺寸 **40 → 22**（`width/height: 22`），圆圈间间距 **8 → 3**（`SizedBox(width: 3)`），两处（PDF 与非 PDF 分支）同步缩小。
  - 页面裁切卡片新增奇偶页分开裁边 `_CropOddEvenToggle`（`0`=统一 / `1`=仅奇数页 / `2`=仅偶数页，绑定 `cropOddEvenMode`/`onCropOddEvenModeChanged`）。
- **本次修改（2026-07-16 晚 · 翻页动画扩展至 9 项）**：`_buildPageTurnSection` 的 `anims` 映射由 `{0:无动画,1:仿真}` 扩为 `{0..8}` 共 9 项（`reader_page_animation_none/simulation/fade/overlap/jump/rotate/carousel/cylinder/flip`）；动画芯片原为单行 `Row`+`Expanded`（仅 2 项），现因 9 项过多改为 `LayoutBuilder`+`Wrap`（每行 3 个，列间距均分、宽 `clamp(80,160)`），新增 `animChip` 非 `Expanded` 网格芯片。`onPageAnimationChanged` 仍回调 `selectedPageAnimation`。
- 依赖/调用 ➔ `settings_controller.dart` / `settings_engine.dart` / `localization_engine.dart`

### `lib/features/shell/ui/book_viewer_page.dart`（PDF 阅读器页）
- **功能**：打开 PDF、聚合 `PdfReaderSettings`、承载 `PdfCustomView` / `PdfReflowView`、同步进度与会话。
- **本次修改**：
  - `_buildPdfView()` 向 `PdfCustomView` 传入 `pageAnimation: SettingsEngine.readerPageAnimation`。
  - `_reflow()` 由「版式组合」改为**真实文本重排**：`await PdfTextReflowService.extract(_pdfDocument)` 提取段落 → 进入 `PdfReflowView`；无文本层（扫描件）提示 `pdf_reflow_empty`；`SettingsController` 异步落库。
  - 新增 `_exitReflow()`；`Expanded` 子区按 `_reflowLoading/_isReflowing/_reflowError` 渲染加载/重排视图/错误提示。
  - `ReaderSettingsSheet` 传入 `showReflow: _isReflowing`。
- **本次修改（2026-07-16 上午 · 9 项需求）**：
  - 重排开/关：沿用 `_reflow()`，已是「在重排中则退出」的切换语义（`_exitReflow` 回版式）；`_readerSettings` getter 聚合 `cropOddEvenMode`。
  - 框选裁边落地：新增 `_showCropSelector`/`_cropStartPos`/`_cropEndPos`/`_cropTargetPage`/`_cropOddEvenMode` 状态与 `_startCropSelection()`/`_confirmCropSelection()`/`_cancelCropSelection()`；`_buildCropSelectorOverlay()` 覆盖层（顶部操作栏「取消 / 页码 ± / 确认」+ 底部提示）内嵌 `_CropSelectorPage`（页面预览 `RawImage` + 手势画框）；确认后归一化选区为 `manualCrop{Left,Right,Top,Bottom}`（%）并 `PdfRenderService.invalidatePageCache` 强制重渲染、`cropMode=2`。**【2026-07-16 第二轮·修复·框选/左右上下裁切不生效】** 原 `_confirmCropSelection` 把选区（页面预览局部坐标）按「全屏幕尺寸」归一化，且 `right/bottom` 公式把分母 `viewW/viewH` 误 `clamp(0.0,0.5)` → `manualCropRight/Bottom` 恒为 0.5，手动裁切完全错位。改为由 `_CropSelectorPage` 在 `BoxFit.contain` 显示矩形内把手势坐标换算为页面图像归一化坐标（0~1）再上报，`_confirmCropSelection` 直接由归一化坐标算四边边距（%）。
  - 奇偶页裁边状态：`_cropOddEvenMode` 初始化自 `SettingsEngine.pdfCropOddEvenMode`，经 `SettingsController.setPdfCropOddEvenMode` 落库并广播（接 `reader_settings_sheet` 的 `_CropOddEvenToggle`）。
  - **【2026-07-16 下午晚 · 手动裁切从未生效 + 框选崩溃修复】**：
    - 手动裁切死代码：原 `reader_settings_sheet._buildCropCard` 中「左右/上下裁切」两个 `_FineTuneSliderRow` 的 `onChanged` 均为 `(_) {}`（死代码），L/R/T/B 仅为只读文本，故滑动无效、数值不变。改为 **左/右/上/下 四个独立 `_FineTuneSliderRow`**，各自 `onChanged` 真实接 `onManualCropLeft/Right/Top/BottomChanged`。
    - 手动裁切不生效的第二层根因：`_PdfPageWidget._load` 的 `useManual` 判定要求 `cropMode == 2`，但原手动滑块回调只改 `_manualCrop*`，未切 `cropMode`。现四个回调在 `_manualCrop* > 0` 首次变化时一并置 `cropMode=2`+`autoCrop=false` 并落库（与框选确认逻辑一致），手动裁切才真正进入渲染链。
    - 框选裁边崩溃（点开即崩）：`_CropSelectorPage` 原在 `build()` 用 `context.size ?? Size.zero` 推导图像显示矩形 `_imageRect`，未布局时为 `Size.zero` → 手势坐标换算 `__localToNorm` 除以零、选区矩形出现 `NaN`/`Infinity` 触发布局断言崩溃。改为 `LayoutBuilder` 拿真实约束 `constraints.biggest` 推导 `_imageRect`，并全程守卫 `_imageRect` 宽高 ≤0 时跳过手势与选区绘制；`_containRect` 增加 `box` 零尺寸守卫。同步 `localization_engine.dart` 新增 `pdf_crop_left/right/top/bottom` 四键。
    - **一键还原按钮（本次新增）**：`ReaderSettingsSheet` 新增 `onResetCrop` 回调；`_buildCropCard` 标题行 `trailing` 放「一键还原」按钮（图标 `CupertinoIcons.refresh` + `pdf_crop_reset` 文案）调用之；`book_viewer_page._resetCrop()` 将 `cropMode=0`/`autoCrop=false`/四边手动边距清零/`cropOddEvenMode=0` 一并 `setState` + 经 `SettingsController` 落库，并遍历所有页 `invalidatePageCache` 强制无裁切重绘。`localization_engine.dart` 新增 `pdf_crop_reset` 键。
- **本次修改（2026-07-16 晚 · 扫描件 OCR 重排接入）**：`_reflow()` 在文本层提取为空时不再直接提示「无可重排文本」，而先判断 `SettingsEngine.pdfOcrEnabled && await PdfOcrService.isModelAvailable()`：成立则置 `_isOcrLoading=true` 并 `await PdfTextReflowService.extractOcr(_pdfDocument, onProgress:)` 逐页 OCR → 进入 `PdfReflowView`；OCR 结果为空提示 `pdf_reflow_ocr_failed`，未内置模型提示 `pdf_reflow_ocr_unavailable`。新增状态字段 `_isOcrLoading`（控制加载文案在 `pdf_reflow_loading`/`pdf_reflow_ocr_loading` 间切换）。`build()` 加载态文案按 `_isOcrLoading` 选择。
- 依赖/调用 ➔ `pdf_custom_view.dart` / `pdf_reflow_view.dart` / `pdf_text_reflow_service.dart` / `pdf_ocr_service.dart` / `pdf_render_service.dart` / `settings_controller.dart` / `settings_engine.dart` / `localization_engine.dart`

### `lib/features/shell/service/pdf_text_reflow_service.dart`（新增 · 真实重排数据源）
- **功能**：基于 PDF 文本层（`PdfPage.loadText().fullText`，已是阅读顺序）逐页提取，按「连字符续行合并 / 句末标点（。！？.!?）断段」聚合成段落，本地、无损、Android+PDF 通用、性能流畅。
- **本次修改**：新增文件。`extract` 返回 `PdfReflowResult{paragraphs, hasTextLayer}`；`hasTextLayer=false`（纯图片扫描件）供上层提示改用 OCR。
- **本次修改（2026-07-16 晚 · OCR 重排数据源）**：新增 `extractOcr(PdfDocument, {onProgress})`——逐页 `page.render(fullWidth:effW, fullHeight:fullH, backgroundColor:white)` → `createImage()` → `ui.Image` → PNG 字节 → `PdfOcrService.recognizePage`，识别文本行以换行拼接后复用 `_appendPageText` 做与文本层一致的续行/断段聚合；`onProgress(current,total)` 上报进度。
- 依赖 ➔ `package:pdfrx` / `dart:ui` / `pdf_ocr_service.dart`

### `lib/features/shell/service/pdf_ocr_service.dart`（OCR 流水线 · Dumb 逻辑层）
- **功能**：基于 `flutter_onnxruntime` 通用 ONNX 绑定，自行实现 PaddleOCR 检测/识别流水线，满足 Windows/iOS/Android/macOS/Web 全平台离线 OCR（模型 assets 内置）。
- **阶段 0（冒烟测试，保留）**：`runAdditionSmokeTest` / `runInferenceFromBytes` 用极简「加法」模型验证 onnxruntime 原生库可加载运行不挂死。
- **本次修改（2026-07-16 晚 · 阶段 3 完整流水线落地）**：
  - `OcrTextLine{text, score, polygon(4 顶点)}` 数据类。
  - `isModelAvailable()`：轻量探测 `ppocr_dict.txt` asset 是否存在，代理判断三模型是否齐备。
  - `recognizePage(Uint8List)`：① `decodeImageFromList` 解码为 `ui.Image` 取 RGBA；② 检测预处理 `_preprocessDet`（长边 960、BGR、ImageNet 归一化、NCHW）经 `det.onnx` 输出概率图；③ `_detectBoxes`（阈值 0.3 + 4 邻接 BFS 连通域 + 包围盒 + 平均分 0.5 过滤 + 缩放比映射回原图）得文本框四边形；④ `_cropAxisAligned` 裁剪文本框 → `_preprocessRec`（高 48、宽 ≤320、NCHW）经 `rec.onnx` 输出；⑤ `_ctcDecode`（逐时间步 argmax + 合并重复 + 去 blank(索引0) + 字典映射）；⑥ `_sortReadingOrder`（按中心 y 行聚类、行内按 x 升序）排成阅读顺序。det/rec 会话与字典静态缓存（整进程只加载一次）。
  - 模型文件约定：需放 `assets/models/{det.onnx, rec.onnx, ppocr_dict.txt}`（与现有 `addition_model.onnx` 同目录）；识别输出布局约定 NCHW=[1,C,T]（C=字典长度），若为 [1,T,C] 需调整 `_runRec` reshape。
- 依赖 ➔ `package:flutter_onnxruntime` / `package:flutter/painting.dart` / `dart:ui` / `dart:math` / `dart:typed_data`

### `lib/features/shell/ui/pdf_reflow_view.dart`（新增 · 重排阅读视图）
- **功能**：将段落以 `Text` 流式排版，`AnimatedBuilder(Listenable.merge([4 个 reflow notifier]))` 实时跟随字号/行距/字距/段距；顶部「退出重排」返回版式。
- 依赖/调用 ➔ `settings_controller.dart`（`pdfReflowFontSize/LineSpacing/LetterSpacing/ParaSpacing`）/ `localization_engine.dart`

### `lib/features/shell/model/pdf_reader_settings.dart`
- **本次修改（2026-07-15 晚）**：`needsRerender => autoCrop || denoise`（去杂色同样改变像素，需重渲染）。
- **本次修改（2026-07-16 上午 · 9 项需求）**：新增 `cropOddEvenMode`(int, 默认 0) 字段 + 构造参数 + `copyWith` + `toString()`；`isAdjusted` 增加 `cropOddEvenMode != 0` 判定；`needsRerender` 增加 `cropOddEvenMode != 0` 判定（奇偶裁边同样改变渲染）。

## 九、PDF 更多设置重组（2026-07-16 凌晨 · 画面增强 + 页面裁切 + 双屏模式）

> 用户要求将 PDF「更多设置」中的分散项归入两个圆角卡片（画面增强 / 页面裁切），滑块增加 [−]/[+] 微调按钮，新增色温、裁切模式切换、双屏对比阅读（2026-07-16 凌晨）。**2026-07-16 上午用户 9 项需求在本节文件基础上续改**：双屏模式真正落地（`pdf_custom_view` 分屏视图）、框选裁边 UI 落地（`book_viewer_page` 覆盖层）、奇偶页分开裁边（`cropOddEvenMode` 全链路）、扫描版居中 / 双页等高 / 连续对齐 / 间隙缩小、智能去白边算法收紧、外观圆圈缩小——各文件「本次修改（2026-07-16 上午）」条目已就地刷新。涉及文件含本节列示 7 个 + `pdf_custom_view.dart`（见第八节）。

### `lib/engine/localization_engine.dart`
- **新增 14 个 i18n key**：`pdf_enhance`, `pdf_enhance_{sharpness,contrast,brightness,saturation,color_temp,remove_color}`, `pdf_crop{,_auto,_manual,_select,_left_right,_top_bottom}`, `pdf_dual_screen{,_desc}`；**【2026-07-16 上午·9 项需求】新增 7 个**：`pdf_reflow_on_desc`（重排开启说明）、`pdf_crop_select_page`（「第 %d 页」）、`pdf_crop_select_hint`（「在页面上拖拽绘制裁切区域」）、`pdf_crop_odd_even_title`/`_all`/`_odd`/`_even`（奇偶页分开裁边：标题/统一/仅奇数页/仅偶数页）。合计 21 个。**【2026-07-16 上午 · 5 新功能续改】新增 16 个**：`reader_landscape`/`_desc`（横屏模式开关 + 说明）、`reader_add_note`（添加笔记）、`reader_add_bookmark`（添加书签）、`reader_bookmarks`（书签 tab/列表标题）、`reader_notes_empty`（笔记空态）、`reader_bookmarks_empty`（书签空态）、`reader_catalog_empty`（目录空态）、`reader_search_placeholder`（搜索框占位）、`reader_search_empty`（搜索无结果）、`reader_searching`（搜索中）、`reader_go_to_page`（跳转页提示）、`reader_note_hint`（笔记输入提示）、`reader_previous_page`/`reader_next_page`（进度 ±1 微调文案）、`reader_search_tab`/`reader_bookmark_tab`（底部导航文案）。累计 37 个。

### `lib/engine/settings_engine.dart`
- **新增 Config Key**（11 个）：`app.reader.pdf.bg.colorTemp`(double,1.0), `app.reader.pdf.cropMode`(int,0), `app.reader.pdf.crop.{left,right,top,bottom}`(double×4,0.0), `app.reader.pdf.dualScreen`(bool,false), **`app.reader.pdf.crop.oddEvenMode`(int,0 · 2026-07-16 上午新增)**。
- **新增 getter/setter** 共 11 组（`pdfBgColorTemp` / `pdfCropMode` / `pdfManualCrop{Left,Right,Top,Bottom}` / `pdfDualScreen` / `pdfCropOddEvenMode`）。

### `lib/features/shell/controller/settings_controller.dart`
- **新增 ValueNotifier**（9 个）：`pdfBgColorTemp`, `pdfCropMode`, `pdfManualCrop{Left,Right,Top,Bottom}`, `pdfDualScreen`, **`pdfCropOddEvenMode`(int · 2026-07-16 上午新增)**。
- **新增 static setter** 共 8 个（`setPdfBgColorTemp` / `setPdfCropMode` / `setPdfManualCrop{Left,Right,Top,Bottom}` / `setPdfDualScreen` / `setPdfCropOddEvenMode`）。

### `lib/features/shell/model/pdf_reader_settings.dart`
- **新增字段**：`cropMode`(int), `manualCropLeft/Right/Top/Bottom`(double×4), `colorTemperature`(double), `dualScreen`(bool), **`cropOddEvenMode`(int · 2026-07-16 上午新增)**。
- 更新 `copyWith`(9 新参数)、`isAdjusted`(含 cropMode/manualCrop/colorTemp/dualScreen/cropOddEvenMode 判定)、`needsRerender`(含 cropMode==2/3/denoise/cropOddEvenMode!=0)、`toString()`。

### `lib/features/shell/service/pdf_render_service.dart`
- **`_colorTemperature(double t)` 色温矩阵方法（2026-07-16 上午第二轮·修复）**：原实现用近似 0 的通道偏移量（最大 ±0.09）合成矩阵，肉眼不可见且非恒等保色变换——表现为「调色温无效果」且「未开去色却整页失色」。重写为 R/G/B 通道**乘性增益**（对角缩放、不改通道间混合）：t>1 暖黄（R×1+0.2·f、G×1+0.05·f、B×1−0.2·f，f=t−1）；t<1 冷蓝（R×1−0.15·f、G×1−0.03·f、B×1+0.2·f，f=1−t）；|t−1|>0.01 时生效，否则 identity。修复后色温可见且不再误清彩色。
- **`buildColorMatrix` 合成链**：亮度 → 色温 → 饱和度 → 对比度 → 灰度（色温在去色之前，故单独调色温不影响彩色通道占比）；null 判定含 `colorTemperature != 1.0`。
  - **【2026-07-16 上午·智能去白边改进】** 详见第八节同文件条目：`_scanContent` 近白阈值 238→242、四周间隙 0.5%→1.5%→**2%（至少 3px，2026-07-16 第二轮·防切内容）**、新增「边缘收紧」四边向内扫描紧贴内容、新增静态 `invalidatePageCache(PdfDocument,int)`（框选裁边确认后强制重渲染）。**手动裁边**见第八节：`renderPageImage` 新增 `manualCrop*` 4 参数，框选优先于自动裁切。

### `lib/features/shell/ui/reader_settings_sheet.dart` ⭐ 核心改动
- **构造参数新增 12 个**：colorTemperature/onColorTemperatureChanged, cropMode/onCropModeChanged, manualCropLeft/Right/Top/Bottom + onChanged, onSelectCrop?, dualScreen/onDualScreenChanged。
- **`_showMoreSettings` 区块完全重写**：
  - **`_StyledCard`**（新 widget）：圆角深色卡片容器（`systemGrey6` 自适应暗色模式），含标题 + 子组件列表。
  - **`_FineTuneSliderRow`**（新 StatefulWidget）：布局为 `标签 | [−] [CupertinoSlider] [+]`；[−]/[+] 按钮以 `step` 步进微调（支持拖动连续调节）；按钮样式为 28×28 圆角方块（`systemGrey5` 背景）。
  - **`_buildEnhanceCard`**：画面增强卡片 — 清晰度(占位)/对比度/亮度/饱和度/色温 五行 `_FineTuneSliderRow` + 去除颜色 SwitchRow + 去杂色 SwitchRow。
  - **`_buildCropCard`**：页面裁切卡片 — 智能自动裁边 SwitchRow（联动 cropMode=1+autoCrop=true）/ **手动四边裁切：左/右/上/下 四个 `_FineTuneSliderRow`，各自 `onChanged` 真实回调 `onManualCropLeft/Right/Top/BottomChanged`（旧实现两行合并滑块 `onChanged: (_) {}` 为死代码、数值为只读文本，故手动裁切从未生效，本次修复）** / 框选裁边 GestureDetector 按钮（onSelectCrop 回调，已由 book_viewer_page 落地）/ **奇偶页分开裁边 `_CropOddEvenToggle`（0=统一/1=仅奇数页/2=仅偶数页，绑定 cropOddEvenMode）** / **标题行右侧「一键还原」按钮（trailing）：调用 `onResetCrop`，还原裁切模式=0、自动裁边关、四边手动边距清零、奇偶页=统一**。**该卡片仅 PDF 读者显示（`if (widget.isPdfReader)`，避免非 PDF 读者点「框选裁边」触发 `widget.document!` 空断言崩溃）**。
  - **`_StyledCard` 新增可选 `trailing` 参数**：标题行右侧渲染任意控件（裁切卡片用于放「一键还原」按钮）。
  - 双屏模式 `_SwitchRow` 独立于两卡片之外。
- **本次修改（2026-07-16 上午 · 9 项需求）**：
  - 重排由 `_ActionRow` 改为 `_SwitchRow`（开→真实文本重排 / 关→退出回版式，`onReflow` 在 `_isReflowing` 时退出）。
  - 外观设置主题配色 / 阅读背景圆圈 **40 → 22**（`width/height: 22`）、圆圈间距 **8 → 3**（`SizedBox(width: 3)`），两处（PDF 与非 PDF 分支）同步缩小。
- 旧 `_SliderRow` widget 保留（未被新 UI 引用，但未删除以避免其他潜在引用断裂）。
- **本次修改（2026-07-16 上午 · 5 新功能续改 · 底部导航重构）**：底部 5 个按钮维持原样（图标/文案/位置/数量不变），新增 `int _selectedNav`（0=目录 / 1=进度 / 2=笔记 / 3=外观 / 4=更多）切换内容区向上生长（沿用现有「画面增强」样式）。`initState` 加载 `document.loadOutline()`（PDF 内置目录）、`ReaderDataStore.loadNotes/loadBookmarks`、`_progressValue`；新增面板：`_buildCatalogPanel`（outline 列表，按 `_flattenOutline` 递归、按深度 `Container(padding: left: 12*depth)` 缩进，点击 `onJumpToPage(dest.pageNumber)`）、`_buildProgressPanel`（页码 `CupertinoTextField` 输入跳转 + `_StyledCard` 内 `_FineTuneSliderRow` 作进度条 + 拖动条两侧 ±1 微调 + 搜索 `CupertinoTextField` 经 400ms 防抖 `loadText().fullText.contains` 串页扫描 + 书签列表增删）、`_buildNotesPanel`（笔记列表 + 底部 `CupertinoButton.filled`「添加笔记」→ `CupertinoAlertDialog` 多行输入）。更多 tab 内新增横屏 `_SwitchRow`（`CupertinoIcons.rotate_right`，`onChanged`→`widget.onToggleLandscape(v)`）；PDF 分支搜索头部图标 `onPressed`→`_selectedNav=1`。构造参数新增：`bookId`(String)/`document`(PdfDocument?)/`totalPages`(int)/`currentPage`(int)/`onJumpToPage`(ValueChanged<int>)/`onToggleLandscape`(ValueChanged<bool>)；非 PDF 读者以空值/占位传入（`_buildProgressPanel` 内 `if (!widget.isPdfReader) return _buildNotSupportedPanel()` 防止无 document 崩溃）。底部导航 `_BottomNavItem` 升级为接受 `onTap`。
- **本次修改（2026-07-16 下午 · 添加笔记弹窗重写）**：原 `_showAddNoteDialog` 在 `CupertinoDialogAction.onPressed` 内 `await` 写盘 + `Navigator.pop` 后 `controller.dispose()`，存在 `pop` 动画期间 `TextField` 仍持有已 dispose 的 `controller` 竞态崩溃，且未捕获写盘异常（异常冒泡到事件句柄直接崩）；弹窗也无主题色（字体/占位符硬编码默认色）。重写为：对话框以 `Navigator.pop(文本或 null)` **返回结果**，写盘移到对话框完全出栈之后、并包 `try/catch`（失败弹非崩溃提示）；`controller.dispose()` 置于 `pop` 返回后；标题/正文/占位符/光标均用 `CupertinoDynamicColor.resolveFrom(context)` 主题化，`maxLines:null`+`minLines:3` 多行输入。同步在 `localization_engine.dart` 补 `confirm` 键（原缺失，`text('confirm')` 回退成字面量 "confirm"）。

### `lib/features/shell/ui/book_viewer_page.dart`
- **新增状态字段 9 个**：`_colorTemperature`, `_cropMode`, `_manualCropLeft/Right/Top/Bottom`, `_dualScreen`, `_cropOddEvenMode`（均初始化自 SettingsEngine）。
- **`_readerSettings` getter 扩展**：聚合全部 9 个新字段到 PdfReaderSettings。
- **ReaderSettingsSheet 构造扩展**：传入全部新回调；**onSelectCrop 已由 `_startCropSelection()` 落地**（不再是 TODO 占位）——见第八节同文件条目「框选裁边落地」；`cropOddEvenMode`/`onCropOddEvenModeChanged` 接 `_CropOddEvenToggle`。
- **本次修改（2026-07-16 上午 · 5 新功能续改 · 跳转/进度/横屏接入）**：新增 `int _currentPage = 1`（由 `_syncProgress` 在进度计算前同步当前页）；新增 `final GlobalKey<PdfCustomViewState> _pdfViewKey`（传给 `PdfCustomView(key:)`）；新增 `void _toggleLandscape(bool enabled)`（`enabled`→`SystemChrome.setPreferredOrientations([landscapeLeft, landscapeRight])`，否则恢复全方向列表）。`ReaderSettingsSheet` 构造新增 `bookId`/`document`/`totalPages`/`currentPage`/`onJumpToPage:(page)=>_pdfViewKey.currentState?.jumpToPage(page)`/`onToggleLandscape:_toggleLandscape`。

### `lib/features/shell/service/reader_data_service.dart`（新增 · 笔记/书签持久化）
- **功能**：按 `bookId` 分文件持久化笔记与书签到应用文档目录（`<appDocs>/book_notes/<safeId>.json` 与 `<appDocs>/book_bookmarks/<safeId>.json`，`safeId = _safeId(bookId)` 过滤非法文件名字符）。
- **模型**：`NoteItem{id, pageNumber, content, createdAt, updatedAt}`（含 `summary` getter 取首行/前 40 字）+ `toJson/fromJson/copyWith`；`BookmarkItem{pageNumber, label, createdAt}` + `toJson/fromJson`。
- **`ReaderDataStore`（静态类）**：`loadNotes/saveNotes/addNote/deleteNote`（笔记按 `pageNumber` 升序）、`loadBookmarks/saveBookmarks/addBookmark`（`addBookmark` 按 `pageNumber` 去重，同一页只保留一个书签）/`deleteBookmark`（书签按 `pageNumber` 升序）。全部读写经 `_file(sub, bookId)` 定位 JSON，异常 try/catch 返回空列表（**可变列表 `<T>[]` 而非 `const []`**，避免后续 `.add()` 抛 "Cannot add to an unmodifiable list"），不阻断阅读器。
- **被消费 ➔** `reader_settings_sheet.dart`（`_buildProgressPanel` 书签增删、`_buildNotesPanel` 笔记列表与添加）。**依赖 ➔** `path_provider`（`getApplicationDocumentsDirectory`）/ `dart:convert`。

### 全局状态 / Key 变动（本轮）
- 新增 Config Key 11 个（见 settings_engine.dart 条目，含 `app.reader.pdf.crop.oddEvenMode`）。
- 新增 ValueNotifier 9 个（见 settings_controller.dart 条目，含 `pdfCropOddEvenMode`）。
- 新增本地化 Key 21 个（见 localization_engine.dart 条目，含 7 个奇偶/框选/重排说明键）。

### 全局状态 / Key 变动（本轮）
- 新增 Config Key：`app.reader.pdf.pageAnimation`（默认 1）、`app.reader.pdf.reflow.fontSize`（18.0）、`app.reader.pdf.reflow.lineSpacing`（1.6）、`app.reader.pdf.reflow.letterSpacing`（0.0）、`app.reader.pdf.reflow.paraSpacing`（8.0）、`app.reader.pdf.ocrEnabled`（默认 true，扫描件 OCR 重排总开关）；均见 `settings_engine.dart` + `settings_controller.dart`。
- 新增本地化 Key：`reader_page_turn_swipe_h/v`、`reader_page_turn_tap_h/v`、`reader_page_turn_tap_scroll`、`reader_page_animation`/`_none`/`_simulation`/`_fade`/`_overlap`/`_jump`/`_rotate`/`_carousel`/`_cylinder`/`_flip`、`pdf_reflow_exit`/`_font_size`/`_line_spacing`/`_letter_spacing`/`_para_spacing`/`_loading`/`_empty`/`_ocr_loading`/`_ocr_unavailable`/`_ocr_failed`；旧 `reader_page_turn_horizontal/vertical/simulation/none` 已删除。
- 能力边界（2026-07-16 晚 更新）：真实重排适用于**含文本层**的 PDF；**纯图片扫描件**现经内置 PP-OCRv5 流水线（`pdf_ocr_service` 阶段3 已落地）直接 OCR 重排，无需提示「无可重排文本」，但需使用者将 `det.onnx`/`rec.onnx`/`ppocr_dict.txt` 放入 `assets/models/`（缺模型时回退提示 `pdf_reflow_ocr_unavailable`）。
