(The file `d:\flutter_application\bookreader\docs\不同文件的依赖关系.md` exists, but is empty)
## 不同文件/模块 的依赖关系（精简、机器友好）

### TXT 阅读页设置面板（2026-07-12）
- `lib/features/shell/ui/txt_viewer_page.dart`
  └─ 变更 ➔ 默认进入全屏阅读状态，点击内容区域弹出底部阅读设置面板；面板交互、遮罩、顶部标题栏和圆角抽屉样式已与 PDF 阅读器保持一致。
  └─ 依赖/调用 ➔ `lib/features/shell/ui/reader_settings_sheet.dart`
  └─ 依赖/调用 ➔ `lib/engine/localization_engine.dart`
- `lib/features/shell/ui/reader_settings_sheet.dart`
  └─ 新增 ➔ 提供与需求一致的底部抽屉式阅读设置 UI，包含主题色块、亮度滑杆、字体按钮、翻页方式按钮与底部导航。
  └─ 变更 ➔ 将面板内边距、分组间距、图标尺寸和按钮高度收紧，降低整体高度约 1/3，避免遮挡并保持可用性。
- `lib/engine/localization_engine.dart`
  └─ 变更 ➔ 新增阅读设置面板相关多语言文案。
- `test/txt_viewer_page_test.dart`
  └─ 变更 ➔ 增加点击内容区域打开设置面板的回归测试，并验证 TXT 设置面板使用与 PDF 相同的圆角抽屉结构。

	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 打开 ➔ `lib/features/shell/ui/book_viewer_page.dart`
- `lib/features/shell/ui/home_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 打开 ➔ `lib/features/shell/ui/book_viewer_page.dart`
- `lib/features/shell/ui/book_viewer_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 变更 ➔ 修复阅读设置弹窗漏传 `onClose` 回调，避免编译失败
	└─ 变更 ➔ 在 PDF 翻页时将当前页码同步回书架进度
	└─ 变更 ➔ 保留顶部返回与标题标签栏，移除底部翻页/页码控制栏，仅在点击中间唤起设置面板时同步显示；关闭设置时一并收起
- `lib/features/shell/ui/txt_viewer_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/ui/reader_settings_sheet.dart`
	└─ 变更 ➔ 修复阅读设置弹窗漏传 `onClose` 回调，避免编译失败
- `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/service/bookshelf_service.dart`
	└─ 变更 ➔ 增加重复导入判断，避免相同文件多次导入；新增重复导入提示文案
	└─ 变更 ➔ 使用短时 toast 弹窗提示重复导入，避免左下角错误文本常驻显示
	└─ 消费 ➔ `lib/features/shell/model/book_model.dart`
- `lib/features/shell/service/bookshelf_service.dart`
	└─ 维护 ➔ 书架书籍列表与阅读进度
- `lib/features/shell/model/book_model.dart`
	└─ 变更 ➔ 增加 `copyWith()` 便于更新进度而不丢失书籍元数据

### 关于页面顶部设计升级（2026-07-09）
- `lib/shared/ui/about_page_header.dart`
	└─ 新增 ➔ 创建新的 `AboutPageHeader` 自定义 Widget
	└─ 功能 ➔ 展示详细的3D蓝色书插图、浮动彩色图标（时钟/统计/书）、微粒装饰、应用标题、中文副标题与版本信息
	└─ 功能 ➔ 浮动图标支持微动画效果（上下浮动）
	└─ 依赖/调用 ➔ `lib/shared/ui/app_text_styles.dart`（文本样式）
	└─ 依赖/调用 ➔ `lib/engine/localization_engine.dart`（本地化副标题）
	└─ 规范 ➔ 所有颜色使用 `CupertinoTheme` 与 `CupertinoColors`，不硬编码
- `lib/features/shell/ui/about_page.dart`
	└─ 修改 ➔ 导入新的 `AboutPageHeader` Widget
	└─ 替换 ➔ 将顶部的普通描述框替换为新的 `AboutPageHeader` 组件
	└─ 移除 ➔ 原有的 `about_app_title`, `about_app_description`, `next_page` 按钮逻辑
- `lib/engine/localization_engine.dart`
	└─ 新增 ➔ `about_app_mission` 多语言键
	└─ 值 ➔ 中文："专注阅读，让每一次打开都值得" | 英文："Focus on reading, make every opening worthwhile."


