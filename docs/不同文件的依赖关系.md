(The file `d:\flutter_application\bookreader\docs\不同文件的依赖关系.md` exists, but is empty)
## 不同文件/模块 的依赖关系（精简、机器友好）

### 书架顶部标题与按钮尺寸统一（2026-07-08）
- `lib/features/shell/ui/bookshelf_page.dart`
	└─ 变更 ➔ 将顶部大标题字号从 28 调整为 20，并使用与主页标题一致的加粗样式
	└─ 变更 ➔ 将搜索与更多按钮图标尺寸统一为 20，颜色改为主题标签色，保持与标题视觉一致
	└─ 依赖/调用 ➔ `lib/engine/localization_engine.dart`（标题文案复用）

### 图片转PDF导出记录"加入书架"真实业务打通（2026-07-08）
- `lib/features/image_to_pdf/ui/image_to_pdf_page.dart`
	└─ 新增导入 ➔ `lib/features/shell/service/bookshelf_service.dart` 用于真实业务
	└─ 修复 ➔ "加入书架"按钮的回调函数，从模拟逻辑改为真实调用 `BookshelfService.importPdf(file)`
	└─ 修复 ➔ 将导出的 PDF 文件真实添加到书架的书籍列表中，更新 `BookshelfService.booksNotifier`
	└─ 修复 ➔ 成功后 UI 通过 `setState` 更新 `_exportRecords` 列表中对应记录的 `addedToShelf` 状态
- `lib/features/shell/service/bookshelf_service.dart`
	└─ 被调用 ➔ `importPdf(File file)` 方法真实将 PDF 添加到书架
	└─ 更新 ➔ 静态 `booksNotifier` ValueNotifier，触发书架 UI 自动刷新
- `lib/features/image_to_pdf/controller/image_to_pdf_controller.dart`
	└─ 作用 ➔ `addExportedPdfToShelf()` 作为中转层，处理异常并持久化 `addedToShelf` 状态
- `lib/features/image_to_pdf/service/image_to_pdf_service.dart`
	└─ 作用 ➔ `updateExportRecord()` 持久化导出记录的 `addedToShelf` 字段到本地 JSON 文件

**【修复验证】**
- ✅ UI 显示"已加入书架"时，书架页面会同步更新显示该 PDF 书籍
- ✅ 刷新应用后，"已加入书架"状态正确持久化
- ✅ 书架的 `booksNotifier` 正确反映导出的 PDF 已被添加

### 图片转PDF导出记录交互完善（2026-07-08）
- `lib/features/image_to_pdf/ui/image_to_pdf_page.dart`
	└─ 新增 ➔ 导入 `open_filex` 包用于打开 PDF 文件
	└─ 完善 ➔ `_buildExportRecordItem()` 中"查看"按钮逻辑，调用 `OpenFilex.open(record.filePath)` 打开系统默认 PDF 阅读器
	└─ 完善 ➔ 文件不存在时使用 `setState` 更新 `_statusMessage` 提示用户
	└─ 完善 ➔ "加入书架"按钮调用 `ImageToPdfController.addExportedPdfToShelf()` 打通 Controller 业务逻辑
	└─ 完善 ➔ 添加成功时通过 `setState` 更新 `_exportRecords` 列表中对应记录的 `addedToShelf` 状态
- `lib/features/image_to_pdf/controller/image_to_pdf_controller.dart`
	└─ 已有 ➔ `addExportedPdfToShelf()` 方法，接收 `addToShelfCallback` 回调用于真实业务入库
	└─ 已有 ➔ 成功时自动调用 `ImageToPdfService.updateExportRecord()` 持久化更新状态
- `lib/features/image_to_pdf/service/image_to_pdf_service.dart`
	└─ 已有 ➔ `updateExportRecord()` 方法支持更新导出记录的各项属性（包括 `addedToShelf` 状态）
- `lib/features/image_to_pdf/model/export_record_model.dart`
	└─ 已有 ➔ `addedToShelf` 字段和 `copyWith()` 方法支持状态更新
- `pubspec.yaml`
	└─ 新增 ➔ `open_filex: ^4.3.2` 依赖用于跨平台打开文件

### 图片转PDF桌面端崩溃修复（2026-07-08）
- `lib/features/image_to_pdf/ui/image_to_pdf_page.dart`
	└─ 修复 ➔ `_buildImagePreviewItem()` 方法中，将外层 `Container` 添加固定宽度 `width: 80`，防止 ListView 中的无限宽度导致渲染引擎崩溃
	└─ 修复 ➔ 移除序号标签 `Container` 的 `width: double.infinity`，改为外层 `Column` 设置 `crossAxisAlignment: CrossAxisAlignment.stretch`
	└─ 影响 ➔ Windows/Mac 桌面端图片导入后立即闪退问题解决
- `lib/features/image_to_pdf/service/filepicker_diagnostics.dart`
	└─ 修复 ➔ 注释掉 `writeLog()` 方法中的文件 I/O 操作（`FileMode.append` + `flush: true`），仅保留 `debugPrint`
	└─ 原因 ➔ 高频循环中的文件写入在 Windows 和 macOS 上导致文件句柄死锁，造成应用崩溃
	└─ 优化 ➔ 若需持久化日志，应使用异步队列或后台隔离区，避免阻塞主线程
- `macos/Runner/DebugProfile.entitlements`
	└─ 新增 ➔ `com.apple.security.files.downloads.read-write: true` - 允许读写下载/文档文件夹
	└─ 新增 ➔ `com.apple.security.files.user-selected.read-write: true` - 允许读写用户选择的文件位置
	└─ 保留 ➔ `com.apple.security.files.user-selected.read-only: true` - 读取用户选择的图片
- `macos/Runner/Release.entitlements`
	└─ 新增 ➔ `com.apple.security.files.downloads.read-write: true` - 允许读写下载/文档文件夹
	└─ 新增 ➔ `com.apple.security.files.user-selected.read-write: true` - 允许读写用户选择的文件位置
	└─ 保留 ➔ `com.apple.security.files.user-selected.read-only: true` - 读取用户选择的图片

### 图片转PDF功能备份（2026-07-08）
- 备份位置：`C:\Users\Admin\Desktop\pdf\image_to_pdf\`
- 备份内容：
  - `lib/features/image_to_pdf/register.dart` - 模块注册
  - `lib/features/image_to_pdf/controller/image_to_pdf_controller.dart` - 控制器
  - `lib/features/image_to_pdf/service/image_to_pdf_service.dart` - 服务层（图片转PDF核心逻辑）
  - `lib/features/image_to_pdf/service/filepicker_diagnostics.dart` - 文件选择器诊断工具
  - `lib/features/image_to_pdf/model/image_to_pdf_model.dart` - 数据模型
  - `lib/features/image_to_pdf/model/export_record_model.dart` - 导出记录模型
  - `lib/features/image_to_pdf/ui/image_to_pdf_page.dart` - UI页面
- 依赖/调用：
  - `package:pdf` (用于生成 PDF)
  - `package:file_picker` (文件选择)
  - `package:image` (图片处理)
  - `package:path_provider` (获取应用文档目录)

### 我的页面占位文案移除（2026-07-08）
- 备份位置：`C:\Users\Admin\Desktop\pdf\image_to_pdf\`
- 备份内容：
  - `lib/features/image_to_pdf/register.dart` - 模块注册
  - `lib/features/image_to_pdf/controller/image_to_pdf_controller.dart` - 控制器
  - `lib/features/image_to_pdf/service/image_to_pdf_service.dart` - 服务层（图片转PDF核心逻辑）
  - `lib/features/image_to_pdf/service/filepicker_diagnostics.dart` - 文件选择器诊断工具
  - `lib/features/image_to_pdf/model/image_to_pdf_model.dart` - 数据模型
  - `lib/features/image_to_pdf/model/export_record_model.dart` - 导出记录模型
  - `lib/features/image_to_pdf/ui/image_to_pdf_page.dart` - UI页面
- 依赖/调用：
  - `package:pdf` (用于生成 PDF)
  - `package:file_picker` (文件选择)
  - `package:image` (图片处理)
  - `package:path_provider` (获取应用文档目录)

### 我的页面占位文案移除（2026-07-08）
- `lib/features/shell/ui/profile_page.dart`
	└─ 变更 ➔ 移除“我的”页面账户设置卡片中的欢迎/占位描述文案，保留会员与同步按钮入口
	└─ 依赖/调用 ➔ `lib/engine/localization_engine.dart`
	└─ 依赖/调用 ➔ `lib/features/membership/ui/membership_page.dart`

### 主页每日一句卡片样式与快速添加（2026-07-08）
- `lib/features/shell/ui/home_page.dart`
	└─ 变更 ➔ 将首页“每日一句”卡片边框颜色调为与阅读统计卡片一致的主题柔和色，并补充轻微阴影
	└─ 变更 ➔ 将右侧编辑按钮替换为加号按钮，点击后通过弹窗快速新增每日一句，并将当前展示内容更新为新增内容
	└─ 依赖/调用 ➔ `lib/features/shell/controller/daily_sentence_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/service/daily_sentence_service.dart`
- `lib/features/shell/controller/daily_sentence_controller.dart`
	└─ 变更 ➔ 复用现有新增逻辑，支持在首页弹窗中直接添加内容

### 我的页面每日一句编辑入口（2026-07-08）
- `lib/features/shell/ui/daily_sentence_page.dart`
	└─ 变更 ➔ 每条已有的每日一句内容右侧新增编辑按钮，点击后跳转到 `lib/features/shell/ui/daily_sentence_edit_page.dart`
- `lib/features/shell/ui/daily_sentence_edit_page.dart`
	└─ 变更 ➔ 复用新增页面逻辑，支持接收已有句子并进入编辑模式
- `lib/features/shell/controller/daily_sentence_controller.dart`
	└─ 变更 ➔ 新增 `updateSentence` 逻辑，用于保存修改后的句子内容
- `lib/features/shell/service/daily_sentence_service.dart`
	└─ 变更 ➔ 新增按 `id` 更新现有句子并持久化的能力

### 我的页面按钮配色（2026-07-08）
- `lib/features/shell/ui/profile_page.dart`
	└─ 变更 ➔ 将“我的”页面中的按钮与设置项卡片背景色调整为与 `lib/features/shell/ui/home_page.dart` 中展示框使用的 `CupertinoTheme.scaffoldBackgroundColor` 保持一致
	└─ 依赖/调用 ➔ `lib/engine/localization_engine.dart`（现有文案复用）
	└─ 依赖/调用 ➔ `lib/features/shell/ui/appearance_page.dart`、`lib/features/shell/ui/settings_page.dart`（页面入口保持原有导航逻辑）
- `lib/features/shell/ui/settings_page.dart`
	└─ 变更 ➔ 语言和外观设置项在该页面中隐藏显示，但保留页面路由与现有入口能力，避免与外层已展示的设置入口重复
	└─ 依赖/调用 ➔ `lib/features/shell/ui/language_page.dart`、`lib/features/shell/ui/appearance_page.dart`
- `lib/features/shell/ui/appearance_page.dart`
	└─ 变更 ➔ 将原先分散的启动页、启动页内容和显示时长配置收敛为一个“启动页设置”入口，点击后进入新的子页面
	└─ 依赖/调用 ➔ `lib/features/shell/ui/splash_settings_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/settings_controller.dart`
- `lib/features/shell/ui/splash_settings_page.dart`
	└─ 新增 ➔ 提供启动页预览与内容类型、图片设置、显示时长、进入方式、跳转页面等配置卡片的统一 UI
	└─ 依赖/调用 ➔ `lib/engine/settings_engine.dart`

### 每日一句 模块（2026-07-08 修改）
- `lib/features/shell/ui/home_page.dart`
	└─ 变更 ➔ `每日一句` 卡片背景改为纯白（显式使用语义颜色 `CupertinoColors.white`），增加外边框 `CupertinoColors.separator`，右下角新增 `编辑` 按钮（打开 `DailySentencePage`）
- `lib/features/shell/ui/daily_sentence_page.dart`
	└─ 变更 ➔ 弹窗输入改为白色底板（`CupertinoPopupSurface`），取消默认 `CupertinoActionSheet` 风格；弹窗底部将 `取消` 与 `保存` 按钮并列放在同一行
- `lib/features/shell/ui/daily_sentence_edit_page.dart`
	└─ 新增 ➔ 全屏添加/编辑页面，取代部分场景下的模态弹窗，提供白色输入区与底部并列按钮布局
- `lib/engine/localization_engine.dart`
	└─ 变更 ➔ 新增多语言键：`edit`（用于卡片右下角编辑按钮）


### 书架顶部统计模块
- `lib/features/shell/ui/bookshelf_page.dart`
  └─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
  └─ 消费 ➔ `lib/features/shell/model/book_model.dart`
  └─ 变更 ➔ 书架顶部统计卡片显示真实数量：全部、收藏、在读、已读
- `lib/features/shell/controller/bookshelf_controller.dart`
  └─ 依赖/调用 ➔ `lib/features/shell/service/bookshelf_service.dart`
  └─ 变更 ➔ 通过 `books` ValueNotifier 向 UI 提供实时统计数据
- `lib/features/shell/service/bookshelf_service.dart`
  └─ 维护 ➔ `booksNotifier` 及书架书籍数据列表
  └─ 变更 ➔ 新增 `isFavorite` 状态支持，供收藏统计使用
- `lib/features/shell/model/book_model.dart`
  └─ 变更 ➔ 新增 `isFavorite` 字段，并支持 `copyWith()` 更新

### 首页最近阅读模块
- `lib/features/shell/ui/home_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 打开 ➔ `lib/features/shell/ui/book_viewer_page.dart`
	└─ 变更 ➔ 首页已重构为卡片式主卡片（参照设计稿），展示“最近阅读”缩略卡、阅读进度条、以及“继续阅读”操作按钮；保留点击跳转阅读逻辑
	└─ 变更 ➔ 新增阅读统计卡片组（时长/书籍/页数/连读天数）与快速功能入口（导入PDF、最近文件、阅读统计、我的收藏）
	└─ 变更 ➔ 将原先的“每日一句”独立为下方卡片，继续使用 `DailySentenceService` 提供数据
	└─ 变更（2026-07-06）➔ 在阅读进度条右侧增加百分比展示，并在卡片顶部补充“最近阅读”标题区
	└─ 变更（2026-07-06 布局优化）➔ 重构首页布局为三层结构：(1)问候区 + 最近阅读卡片，(2)中间阅读数据展示（大号时长卡片 + 三个统计卡片），(3)快捷功能 + 每日一句
	└─ 变更（2026-07-06 新增方法）➔ 新增 `_readingDataSection()` 方法展示总阅读时长与统计数据，新增 `_buildStatCard()` 辅助方法构建单个统计卡片
	└─ 变更（2026-07-06 首页文案）➔ 在问候区使用“下午好，万志豪！”作为占位标题，文字比副标题更大且加粗，依赖 `lib/engine/localization_engine.dart`
	└─ 变更（2026-07-07 间距收紧）➔ 将首页“问候卡片”与“继续阅读”卡片之间的间距收紧为 2 像素，保持两段内容的视觉连续性；并通过列表分隔器与外层内边距共同控制，避免出现额外的视觉留白
	└─ 变更（2026-07-07 按钮高度）➔ 将首页“继续阅读”按钮整体增高，并保持下边缘位置不变，让按钮上边缘更靠近进度条
	└─ 变更（2026-07-07 进度条位置）➔ 将首页进度条整体下移至“继续阅读”按钮上方，保持 3 像素间距，百分比仍与进度条中心对齐
	└─ 变更（2026-07-07 标题排版）➔ 将首页“最近阅读”标题缩小并移入卡片内部，内容整体向下排布以靠近卡片下边缘，同时保持卡片整体高度不变
- `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/service/bookshelf_service.dart`
	└─ 消费 ➔ `lib/features/shell/model/book_model.dart`
- `lib/engine/localization_engine.dart`
	└─ 新增 ➔ `recently_reading`, `no_recently_reading` 多语言文案
	└─ 新增（2026-07-06）➔ `monthly_reading`, `yearly_reading` 多语言文案（用于首页统计卡片）

### 书架与PDF相关交互气泡弹窗
- `lib/features/shell/ui/bookshelf_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/ui/book_viewer_page.dart`
	└─ 变更 ➔ 书架“更多”菜单与书籍长按菜单改为基于锚点的悬浮气泡弹窗（Popover）
	└─ 变更 ➔ 取消全屏动作表式交互，改为局部悬浮层
	└─ 变更 ➔ 将书架弹窗宽度改为基于最长菜单文本动态计算，避免固定宽度过宽
	└─ 变更 ➔ 统计卡片和书架列表卡片改为使用主题背景色，修复暗色模式白底问题
- `lib/features/image_to_pdf/ui/image_to_pdf_page.dart`
	└─ 变更 ➔ 长按图片预览时，删除确认由全屏弹窗改为锚点悬浮气泡弹窗

### 图像转PDF 功能
- `lib/features/image_to_pdf/controller/image_to_pdf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/image_to_pdf/service/image_to_pdf_service.dart`
	└─ 依赖/调用 ➔ `lib/features/image_to_pdf/service/filepicker_diagnostics.dart`
	└─ 产出 ➔ `lib/features/image_to_pdf/model/export_record_model.dart`

- `lib/features/image_to_pdf/service/image_to_pdf_service.dart`
	└─ 依赖/调用 ➔ `package:pdf` (用于生成 PDF)
	└─ 读写 ➔ 应用文档目录（通过 `path_provider` 获取）

### TXT转EPUB 功能
- `lib/features/txt_to_epub/controller/txt_to_epub_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/txt_to_epub/service/txt_to_epub_service.dart`
	└─ 产出 ➔ `lib/features/txt_to_epub/model/txt_to_epub_model.dart`

- `lib/features/txt_to_epub/service/txt_to_epub_service.dart`
	└─ 依赖/调用 ➔ `package:flutter` (compute 函数用于后台处理)
	└─ 读写 ➔ 应用文档目录（通过 `path_provider` 获取）

- `lib/features/txt_to_epub/ui/txt_to_epub_page.dart`
	└─ 依赖/调用 ➔ `lib/features/txt_to_epub/controller/txt_to_epub_controller.dart`
	└─ 消费 ➔ `lib/features/txt_to_epub/model/txt_to_epub_model.dart`

### DOC转PDF 功能
- `lib/features/doc_to_pdf/controller/doc_to_pdf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/doc_to_pdf/service/doc_to_pdf_service.dart`
	└─ 产出 ➔ `lib/features/doc_to_pdf/model/doc_to_pdf_model.dart`

- `lib/features/doc_to_pdf/service/doc_to_pdf_service.dart`
	└─ 依赖/调用 ➔ `package:pdf` (生成高质量 PDF)
	└─ 依赖/调用 ➔ `package:archive` (解析 DOCX 文件)
	└─ 依赖/调用 ➔ `package:flutter` (compute 函数用于后台处理)
	└─ 纯 Dart 实现，无需系统工具（LibreOffice/Pandoc）
	└─ 读写 ➔ 应用文档目录（通过 `path_provider` 获取）
	└─ 读写 ➔ `doc2pdf_export_records.json` (本地持久化转换记录)

- `lib/features/doc_to_pdf/ui/doc_to_pdf_page.dart`
	└─ 依赖/调用 ➔ `lib/features/doc_to_pdf/controller/doc_to_pdf_controller.dart`
	└─ 消费 ➔ `lib/features/doc_to_pdf/model/doc_to_pdf_model.dart`

### PPT转PDF 功能
- `lib/features/ppt_to_pdf/controller/ppt_to_pdf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/ppt_to_pdf/service/ppt_to_pdf_service.dart`
	└─ 产出 ➔ `lib/features/ppt_to_pdf/model/ppt_to_pdf_model.dart`

- `lib/features/ppt_to_pdf/service/ppt_to_pdf_service.dart`
	└─ 依赖/调用 ➔ `package:pdf` (生成 PDF)
	└─ 依赖/调用 ➔ `package:archive` (解析 PPTX ZIP)
	└─ 依赖/调用 ➔ `package:flutter` (compute 函数用于后台处理)
	└─ 纯 Dart 实现，无需系统工具（LibreOffice/Pandoc）
	└─ 读写 ➔ 应用文档目录（通过 `path_provider` 获取）
	└─ 读写 ➔ `ppt2pdf_export_records.json` (本地持久化转换记录)

- `lib/features/ppt_to_pdf/ui/ppt_to_pdf_page.dart`
	└─ 依赖/调用 ➔ `lib/features/ppt_to_pdf/controller/ppt_to_pdf_controller.dart`
	└─ 消费 ➔ `lib/features/ppt_to_pdf/model/ppt_to_pdf_model.dart`

### Excel转PDF 功能
- `lib/features/excel_to_pdf/controller/excel_to_pdf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/excel_to_pdf/service/excel_to_pdf_service.dart`
	└─ 产出 ➔ `lib/features/excel_to_pdf/model/excel_to_pdf_model.dart`

- `lib/features/excel_to_pdf/service/excel_to_pdf_service.dart`
	└─ 依赖/调用 ➔ `package:pdf` (生成 PDF)
	└─ 依赖/调用 ➔ `package:archive` (解析 XLSX ZIP)
	└─ 依赖/调用 ➔ `package:flutter` (compute 函数用于后台处理)
	└─ 纯 Dart 实现，无需系统工具（LibreOffice/Pandoc）
	└─ 读写 ➔ 应用文档目录（通过 `path_provider` 获取）
	└─ 读写 ➔ `excel2pdf_export_records.json` (本地持久化转换记录)

- `lib/features/excel_to_pdf/ui/excel_to_pdf_page.dart`
	└─ 依赖/调用 ➔ `lib/features/excel_to_pdf/controller/excel_to_pdf_controller.dart`
	└─ 消费 ➔ `lib/features/excel_to_pdf/model/excel_to_pdf_model.dart`

### 工具页面 集成
- `lib/features/shell/ui/tools_page.dart`
	└─ 导入/打开 ➔ `lib/features/image_to_pdf/ui/image_to_pdf_page.dart`
	└─ 导入/打开 ➔ `lib/features/txt_to_epub/ui/txt_to_epub_page.dart`
	└─ 导入/打开 ➔ `lib/features/doc_to_pdf/ui/doc_to_pdf_page.dart`
 	└─ 导入/打开 ➔ `lib/features/ppt_to_pdf/ui/ppt_to_pdf_page.dart`
 	└─ 导入/打开 ➔ `lib/features/excel_to_pdf/ui/excel_to_pdf_page.dart`

### 书架导入 功能
- `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/service/bookshelf_service.dart`
	└─ 使用 ➔ `file_picker`（选择 PDF）
- `lib/features/shell/service/bookshelf_service.dart`
	└─ 变更 ➔ 在导入时保存真实文件名、文件大小与封面数据到 `lib/features/shell/model/book_model.dart`
- `lib/features/shell/model/book_model.dart`
	└─ 变更 ➔ 新增 `fileSizeBytes` 字段并支持 `copyWith()` 更新
- `lib/features/shell/ui/bookshelf_page.dart`
	└─ 变更 ➔ 使用真实书名和文件大小渲染书架卡片元信息

### 书架随机读书 功能
- `lib/features/shell/ui/bookshelf_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 打开 ➔ `lib/features/shell/ui/book_viewer_page.dart`
	└─ 显示 ➔ 空书架导入按钮
	└─ 提供 ➔ 封面卡片展示、长按菜单、右下角操作入口
- `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/service/bookshelf_service.dart`
	└─ 消费 ➔ `lib/features/shell/model/book_model.dart`
	└─ 提供 ➔ 删除书籍能力
- `lib/features/shell/service/bookshelf_service.dart`
	└─ 维护 ➔ 当前书架书籍列表
	└─ 提供 ➔ 随机选择书籍能力
	└─ 提供 ➔ 删除指定书籍能力

### 回忆页面 阅读统计展示
- `lib/features/shell/ui/memory_page.dart`
	└─ 提供 ➔ 阅读统计页面骨架、周期切换、摘要卡片、图表卡片、趋势分析卡片与等高统计网格
	└─ 依赖/调用 ➔ `lib/engine/localization_engine.dart`（多语言文案）
	└─ 变更 ➔ 将原有日历占位页升级为更清晰的纵向阅读统计界面，采用统一三列等高卡片布局，并将趋势总结置于卡片组下方
	└─ 变更 ➔ 总阅读时长卡片标题加粗，图形切换按钮置于右侧，总阅读时长下方依次显示：总时长统计、日均时长、上一周期变化率
	└─ 变更（2026-07-07 调整）➔ 将图表下方的选中点数据显示位置调整到总阅读时长卡片下方，避免在窄屏或布局变化时被图表覆盖
	└─ 变更（2026-07-07 本地化）➔ 当应用语言为中文时，总阅读时长显示格式改为“XX小时XX分钟”，并将字体尺寸适当调小以适应布局
	└─ 变更 ➔ 顶部日期显示改为真实当前日期和当前周期范围
- `lib/engine/localization_engine.dart`
	└─ 新增 ➔ 回忆页面趋势分析与摘要文案键值
	└─ 新增 ➔ `vs_previous_period` 多语言文案键值

### 首页阅读统计卡片微调（2026-07-08）
- `lib/features/shell/ui/home_page.dart`
	└─ 变更 ➔ 将首页三个统计卡片的高度调整为最小高度 46，并改为支持自适应高度（使用 `BoxConstraints(minHeight: 46)`），以在内容较多时自动展开避免文字与边框重叠
	└─ 变更 ➔ 保持标签置于数值上方及第三个统计卡片使用 `cumulative_reading` 文案，确保中文环境下显示为“累计阅读”
	└─ 变更 ➔ 保持阅读统计大卡片标题左上展示、加粗，主数据字号相对缩小以配合卡片组整体视觉
	└─ 变更 ➔ 在阅读统计卡片右侧继续展示“连续阅读”块（火花图标与天数数值）
	└─ 依赖/调用 ➔ `lib/engine/localization_engine.dart`
- `lib/engine/localization_engine.dart`
  └─ 依赖/调用 ➔ `lib/engine/settings_engine.dart`
  └─ 变更 ➔ 提供 `cumulative_reading` 的多语言文案，确保主页统计卡片在中英文环境下正确显示

### 全局/跨模块 注意点
- 权限检查与文件选择：各转换Feature中使用 `permission_handler` 与 `file_picker`，在 Android 13+ 需声明相应权限。
- 导出记录持久化：各Feature都在应用文档目录中保存自己的转换记录JSON文件。
- txt2epub_export_records.json：存储TXT转EPUB的转换记录
- doc2pdf_export_records.json：存储DOC转PDF的转换记录
- export_records.json：存储图片转PDF的转换记录

-- 记录生成于自动化脚本

### 首页顶部操作按钮（2026-07-07）
- `lib/features/shell/ui/home_page.dart`
  └─ 依赖/调用 ➔ `lib/features/shell/controller/settings_controller.dart`
  └─ 依赖/调用 ➔ `lib/engine/settings_engine.dart`
  └─ 变更 ➔ 将首页标题移至导航栏左侧，并在右侧新增语言切换与主题模式切换按钮
  └─ 变更 ➔ 语言按钮在中文/英文之间切换，主题按钮仅显示图标并切换浅/深色模式

### 书架页面重设计（2026-07-05）
- `lib/features/shell/ui/bookshelf_page.dart`
	└─ 变更：整体重设计 UI，新增顶部统计卡片组（全部/收藏/在读/已读）、“最近阅读”横向缩略卡、以及分类段控（全部/PDF/EPUB/TXT/其他）；保留原有导入、随机阅读、长按菜单等交互逻辑
	└─ 变更（2026-07-05 细化）：分类段控调整为横向五等分布局，按钮同时显示图标与文字，选中项使用椭圆边框与主色高亮，右侧“过滤”图标去除厚重灰色背景（由灰色方块改为简洁图标）
	└─ 修复（2026-07-07）：修复分类筛选逻辑未生效的问题。筛选现在先根据 `lib/features/shell/ui/bookshelf_page.dart` 中的 `_selectedCategory` 对书籍类型进行过滤（包含 `other` 分组），然后再应用搜索关键字过滤，确保选择 `txt` 时只显示 `.txt` 类型的书籍。
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 打开 ➔ `lib/features/shell/ui/book_viewer_page.dart`
	└─ 显示 ➔ 网格/列表两种视图（封面/列表）及自定义进度条
	└─ 注意 ➔ 新增多语言键：`bookshelf_tab_all`, `bookshelf_tab_other`, `recently_reading`, `view_all`, `no_recently_reading`（请同步更新 `lib/engine/localization_engine.dart` 的多语言资源）

### 书架最近阅读与阅读进度同步（2026-07-06）
- `lib/features/shell/ui/bookshelf_page.dart`
	└─ 变更 ➔ 移除单本书重复显示 4 次的最近阅读卡片逻辑
	└─ 变更 ➔ 将“全部书籍”网格卡片重构为紧凑的横向 Book Card，封面左置、文字右置、右上角 More 按钮、去除进度条
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 打开 ➔ `lib/features/shell/ui/book_viewer_page.dart`
- `lib/features/shell/ui/home_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 打开 ➔ `lib/features/shell/ui/book_viewer_page.dart`
- `lib/features/shell/ui/book_viewer_page.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 变更 ➔ 在 PDF 翻页时将当前页码同步回书架进度
- `lib/features/shell/controller/bookshelf_controller.dart`
	└─ 依赖/调用 ➔ `lib/features/shell/service/bookshelf_service.dart`
	└─ 消费 ➔ `lib/features/shell/model/book_model.dart`
- `lib/features/shell/service/bookshelf_service.dart`
	└─ 维护 ➔ 书架书籍列表与阅读进度
- `lib/features/shell/model/book_model.dart`
	└─ 变更 ➔ 增加 `copyWith()` 便于更新进度而不丢失书籍元数据

